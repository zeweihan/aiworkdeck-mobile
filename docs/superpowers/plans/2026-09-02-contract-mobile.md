# 四端共享契约（移动仓）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在移动仓建立 `contract/` 单一来源，让 iOS 与小程序从它生成纯数据、用同一套夹具校验行为，并让规矩在会话加载、编辑、提交、CI 四层被自动触发。

**Architecture:** 四个 JSON（状态机 / 文案 / 令牌 / 能力）+ 五个夹具是真源；`gen.mjs` 出四端纯数据文件（进仓提交），`check.mjs` 校验生成物未过期、夹具合 schema、引用闭合、无内联文案、API 副本未漂。各端行为（状态迁移、计数、删除警告、status 合并）手写成纯函数，跑同一套夹具。

**Tech Stack:** Node 22（`node --test` 直跑 .ts）、ajv 8（夹具 schema）、Swift/XCTest、xcodegen、GitHub Actions。

**Spec:** `docs/specs/2026-09-02-contract-design.md`

## Global Constraints

- 内部状态正式名：`waiting / uploading / uploaded / arrived / failed`；iOS 旧值 `moving` 只解码不编码（C4）。
- 三桶：`uploading / staged / landed`，桶名文案「上传中 / 已暂存 / 已落盘」；`failed ⊆ uploading`。
- 令牌名 `S.waiting / S.moving / S.arrived / S.failed` 不改；单位基准 pt，wxss 间距 ×2 出 rpx，字号统一 px。
- 取证主流程界面文案不许内联，走 `strings.json` 键；`en` 栏从第一天存在。
- 改动顺序：先改 `contract/*.json` 与 `contract/fixtures/`，跑 `node contract/tools/gen.mjs`，再改各端行为代码；生成物进仓。
- 生成物文件头必须含 `GENERATED by contract/tools/gen.mjs`。
- 所有提交信息末尾加 `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`。
- 本计划在 worktree 里执行时先 `npm ci`（否则 typecheck 假绿，见项目记忆 mobile-local-verify-recipe）。
- Task 8 依赖服务端计划 `2026-09-02-contract-server-openapi.md` 完成；其余任务不依赖服务端。

---

## 文件结构

| 路径 | 职责 |
|---|---|
| `contract/contract.json` | 版本 + 四个数据文件的 sha256（由 gen 维护，check 校验） |
| `contract/transfer-state.json` | 五态、别名、三桶、迁移表、重试延迟、删除警告等级 |
| `contract/strings.json` | 文案词典 key → {zh-Hans, en} |
| `contract/tokens.json` | 设计令牌 |
| `contract/capabilities.json` | 四端能力声明 |
| `contract/schema/*.schema.json` | 五个夹具的 JSON Schema |
| `contract/fixtures/*.json` | 黄金向量 |
| `contract/tools/lib.mjs` | 加载契约、渲染四端输出（gen 与 check 共用） |
| `contract/tools/gen.mjs` | 写出生成物 + 刷新 contract.json |
| `contract/tools/check.mjs` | 五项校验 |
| `contract/tools/pull-api.mjs` | 从服务端仓拉 OpenAPI 并写 PINNED.json |
| `contract/tools/hook-post-edit.mjs` | Claude Code PostToolUse 钩子入口 |
| `contract/tools/wxss-footer.css` | tokens.wxss 尾部的三个工具类（非令牌，手维护） |
| `contract/README.md` | 改契约的操作步骤 |
| `miniprogram/utils/contract/{states,strings,capabilities}.ts` | 生成物 |
| `miniprogram/utils/i18n.ts` | `t(key, vars)`，手写 |
| `miniprogram/utils/transition.ts` | `next / recover / applyStatus` 纯函数，手写 |
| `miniprogram/styles/tokens.wxss` | 生成物（原地覆盖） |
| `ios/Sources/Contract/{Tokens,Strings,States,Capabilities}.swift` | 生成物 |
| `ios/Sources/Design/Typography.swift` | `T.F / T.A / Color(hex:)`，从旧 Tokens.swift 拆出，手写 |
| `ios/Sources/Design/L10n.swift` | `tr(key, vars)`，手写 |
| `android/contract/src/main/kotlin/com/aiworkdeck/contract/*.kt` | 生成物（安卓项目建立前先存在） |
| `harmony/contract/src/main/ets/*.ets` | 生成物 |
| `tests/contract-tools.test.ts` | gen/check 自身测试 |
| `tests/contract.test.ts` | 小程序夹具适配 |
| `ios/Tests/ContractFixturesTests.swift` | iOS 夹具适配 |
| `CLAUDE.md`、`.claude/skills/contract-change/SKILL.md`、`.claude/settings.json`、`scripts/install-hooks.mjs`、`.github/workflows/contract.yml`、`.github/pull_request_template.md` | 五层触发 |

---

### Task 1: 契约数据文件与清单

**Files:**
- Create: `contract/transfer-state.json`、`contract/strings.json`、`contract/tokens.json`、`contract/capabilities.json`、`contract/contract.json`
- Create: `contract/tools/lib.mjs`（本任务只做 `loadContract` 与 `sha256`）
- Test: `tests/contract-tools.test.ts`

**Interfaces:**
- Produces: `loadContract(root) → { root, state, strings, tokens, caps, manifest }`；`sha256(text) → hex`；`DATA_FILES = ['transfer-state.json','strings.json','tokens.json','capabilities.json']`

- [ ] **Step 1: 写失败测试**

`tests/contract-tools.test.ts`：

```ts
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { readFileSync } from 'node:fs'
import { join } from 'node:path'
import { loadContract, sha256, DATA_FILES } from '../contract/tools/lib.mjs'

const ROOT = join(import.meta.dirname, '..')

test('contract.json 的 sha 与四个数据文件一致', () => {
  const c = loadContract(ROOT)
  for (const f of DATA_FILES) {
    const actual = sha256(readFileSync(join(ROOT, 'contract', f), 'utf8'))
    assert.equal(c.manifest.files[f], actual, `${f} 的 sha 过期，跑 node contract/tools/gen.mjs`)
  }
})

test('状态机引用闭合', () => {
  const { state, strings, tokens } = loadContract(ROOT)
  const states = new Set(state.states)
  for (const [alias, to] of Object.entries(state.aliases)) {
    assert.ok(states.has(to), `别名 ${alias} 指向未知状态 ${to}`)
  }
  const covered = new Set()
  for (const [phase, p] of Object.entries(state.phases)) {
    for (const s of p.states) { assert.ok(states.has(s), `${phase} 含未知状态 ${s}`); covered.add(s) }
    assert.ok(strings[p.label], `桶 ${phase} 的文案键 ${p.label} 不存在`)
    const [grp, name] = p.dot.split('.')
    assert.ok(tokens[grp]?.[name], `桶 ${phase} 的点色令牌 ${p.dot} 不存在`)
  }
  assert.deepEqual([...covered].sort(), [...states].sort(), '每个状态必须恰属一个桶')
  for (const s of state.states) {
    assert.ok(strings[state.stateText[s]], `stateText[${s}] 文案键不存在`)
    assert.ok(strings[state.whereItIs[s]], `whereItIs[${s}] 文案键不存在`)
  }
  for (const t of state.transitions) {
    assert.ok(states.has(t.from) && states.has(t.to), `迁移 ${t.from}→${t.to} 含未知状态`)
  }
  assert.equal(state.maxAutoRetries, state.retryDelaysMs.length)
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/contract-tools.test.ts`
Expected: FAIL，`Cannot find module '../contract/tools/lib.mjs'`

- [ ] **Step 3: 写四个数据文件**

`contract/transfer-state.json`：

```json
{
  "version": 1,
  "states": ["waiting", "uploading", "uploaded", "arrived", "failed"],
  "aliases": { "moving": "uploading" },
  "phases": {
    "uploading": { "states": ["waiting", "uploading", "failed"], "label": "phase.uploading", "dot": "S.waiting" },
    "staged":    { "states": ["uploaded"], "label": "phase.staged", "dot": "S.moving" },
    "landed":    { "states": ["arrived"],  "label": "phase.landed", "dot": "S.arrived" }
  },
  "failedDot": "S.failed",
  "stateText": {
    "waiting": "state.uploading",
    "uploading": "state.uploading",
    "failed": "state.failed",
    "uploaded": "state.uploaded.detail",
    "arrived": "state.landed"
  },
  "whereItIs": {
    "waiting": "where.waiting",
    "uploading": "where.uploading",
    "uploaded": "where.uploaded",
    "arrived": "where.arrived",
    "failed": "where.failed"
  },
  "retryDelaysMs": [5000, 15000, 45000],
  "maxAutoRetries": 3,
  "events": ["kick", "http_2xx", "http_error", "network_error", "retry_manual", "retry_auto", "status_delivered", "status_pending", "app_launch"],
  "transitions": [
    { "from": "waiting",   "event": "kick",             "to": "uploading" },
    { "from": "uploading", "event": "http_2xx",         "to": "uploaded" },
    { "from": "uploading", "event": "http_error",       "to": "failed" },
    { "from": "uploading", "event": "network_error",    "to": "failed" },
    { "from": "failed",    "event": "retry_manual",     "to": "waiting" },
    { "from": "failed",    "event": "retry_auto",       "to": "waiting", "guard": "attempts <= maxAutoRetries" },
    { "from": "uploaded",  "event": "status_delivered", "to": "arrived" },
    { "from": "uploaded",  "event": "status_pending",   "to": "uploaded" },
    { "from": "uploading", "event": "app_launch",       "to": "waiting" },
    { "from": "failed",    "event": "app_launch",       "to": "waiting", "guard": "attempts <= maxAutoRetries" }
  ],
  "deleteWarning": {
    "order": ["uploading", "staged", "landed"],
    "levels": { "uploading": "unsent", "staged": "staged", "landed": "landed" },
    "keys": { "unsent": "delete.warn.unsent", "staged": "delete.warn.staged", "landed": "delete.warn.landed" }
  }
}
```

`contract/strings.json`（占位符统一 `{name}`）：

```json
{
  "phase.uploading":        { "zh-Hans": "上传中", "en": "Uploading" },
  "phase.staged":           { "zh-Hans": "已暂存", "en": "Staged" },
  "phase.landed":           { "zh-Hans": "已落盘", "en": "Landed" },
  "state.uploading":        { "zh-Hans": "上传中", "en": "Uploading" },
  "state.failed":           { "zh-Hans": "上传失败", "en": "Upload failed" },
  "state.uploaded":         { "zh-Hans": "已暂存", "en": "Staged" },
  "state.uploaded.detail":  { "zh-Hans": "已暂存 · 等待桌面端接收", "en": "Staged · waiting for desktop" },
  "state.landed":           { "zh-Hans": "已落盘", "en": "Landed" },
  "where.waiting":          { "zh-Hans": "在手机上", "en": "On this phone" },
  "where.uploading":        { "zh-Hans": "在路上", "en": "In transit" },
  "where.uploaded":         { "zh-Hans": "在中转区，等电脑取回", "en": "In relay, waiting for your computer" },
  "where.arrived":          { "zh-Hans": "在你电脑上", "en": "On your computer" },
  "where.failed":           { "zh-Hans": "需重试", "en": "Needs retry" },
  "tally.failedSuffix":     { "zh-Hans": "含 {m} 失败", "en": "{m} failed" },
  "tally.summary":          { "zh-Hans": "上传中 {u} 张{f}，已暂存 {s} 张，已落盘 {l} 张", "en": "{u} uploading{f}, {s} staged, {l} landed" },
  "tally.summary.failedPart": { "zh-Hans": "（含 {m} 失败）", "en": " ({m} failed)" },
  "queue.section.uploading": { "zh-Hans": "上传中", "en": "Uploading" },
  "queue.section.staged":   { "zh-Hans": "已暂存 · 等电脑取回", "en": "Staged · waiting for your computer" },
  "queue.section.landed":   { "zh-Hans": "已落盘", "en": "Landed" },
  "delete.title":           { "zh-Hans": "删除 {n} 件", "en": "Delete {n} items" },
  "delete.warn.unsent":     { "zh-Hans": "其中 {n} 件还没送出去。删了就没了，无法找回。", "en": "{n} of these have not been sent. Deleting them is permanent." },
  "delete.warn.staged":     { "zh-Hans": "其中 {n} 件电脑还没取回。中转区 7 天后清理，电脑若未及时接收，这些影像将无法找回。", "en": "{n} of these have not reached your computer. The relay clears after 7 days; if the desktop does not collect them in time they cannot be recovered." },
  "delete.warn.landed":     { "zh-Hans": "删除 {n} 件本地原图？电脑上已有副本。", "en": "Delete {n} local originals? Your computer already has copies." },
  "cap.noBackgroundUpload": { "zh-Hans": "本端不支持后台上传，请保持页面打开直到上传完成", "en": "This client cannot upload in the background. Keep the page open until uploads finish." },
  "cap.segmentedRecording": { "zh-Hans": "本端录像为记录用途，证据用途请用 iOS 端", "en": "Video on this client is for record-keeping. Use the iOS app for evidentiary recording." },
  "empty.projects":         { "zh-Hans": "桌面端用同一手机号登录并保持运行，项目约一分钟内出现在这里", "en": "Sign in to the desktop app with the same phone number and keep it running. Projects appear here within about a minute." },
  "error.uploadFailed":     { "zh-Hans": "上传失败（{code}）", "en": "Upload failed ({code})" }
}
```

`contract/tokens.json`：

```json
{
  "version": 1,
  "unit": { "base": "pt", "wxssSpacingFactor": 2, "typeUnit": "px" },
  "L": { "bg": "#FFFFFF", "sunken": "#FAFAFA", "fg": "#0A0E1A", "fgMuted": "#6B7280", "fgFaint": "#9CA3AF",
         "rule": "#ECEEF1", "ruleStrong": "#D4D8DE", "accent": "#1E3A8A", "accentWash": "#F4F6FB" },
  "D": { "bg": "#0A0B0D", "surface": "#141619", "fg": "#F5F6F7", "fgMuted": "#8A8F98", "rule": "rgba(255, 255, 255, 0.10)" },
  "S": { "waiting": "#C2410C", "moving": "#1E3A8A", "arrived": "#15803D", "failed": "#B91C1C",
         "waitingOnDark": "#F97316", "movingOnDark": "#60A5FA", "arrivedOnDark": "#4ADE80" },
  "Sp": { "s1": 4, "s2": 8, "s3": 12, "s4": 16, "s5": 20, "s6": 24, "s8": 32, "s10": 40, "s16": 64, "gutter": 20 },
  "Ty": { "hero": 44, "display": 30, "title": 21, "heading": 16, "body": 15, "small": 13, "micro": 11, "nano": 10 },
  "touchMin": 44,
  "motion": { "fastMs": 140, "baseMs": 220, "slowMs": 320,
              "easeOut": "cubic-bezier(0.22, 1, 0.36, 1)", "easeIn": "cubic-bezier(0.55, 0, 1, 0.45)" },
  "font": {
    "sans": "system-ui, -apple-system, \"PingFang SC\", \"HarmonyOS Sans SC\", \"Source Han Sans SC\", \"Helvetica Neue\", sans-serif",
    "mono": "\"SF Mono\", ui-monospace, Menlo, Consolas, monospace"
  },
  "wxssNames": {
    "L": { "bg": "bg", "sunken": "bg-sunken", "fg": "fg", "fgMuted": "fg-muted", "fgFaint": "fg-faint",
           "rule": "rule", "ruleStrong": "rule-strong", "accent": "accent", "accentWash": "accent-wash" },
    "D": { "bg": "dk-bg", "surface": "dk-surface", "fg": "dk-fg", "fgMuted": "dk-fg-muted", "rule": "dk-rule" },
    "S": { "waiting": "st-waiting", "moving": "st-moving", "arrived": "st-arrived", "failed": "st-failed",
           "waitingOnDark": "st-waiting-dk", "movingOnDark": "st-moving-dk", "arrivedOnDark": "st-arrived-dk" }
  }
}
```

`contract/capabilities.json`：

```json
{
  "version": 1,
  "clients": ["ios", "miniprogram", "android", "harmony"],
  "capabilities": {
    "backgroundUpload":   { "ios": true,  "miniprogram": false,     "android": true,  "harmony": true,
                            "degradedNotice": "cap.noBackgroundUpload" },
    "maxVideoSeconds":    { "ios": null,  "miniprogram": 60,        "android": null,  "harmony": null },
    "continuousSegments": { "ios": false, "miniprogram": true,      "android": false, "harmony": false,
                            "degradedNotice": "cap.segmentedRecording" },
    "glassBlur":          { "ios": true,  "miniprogram": "runtime", "android": "runtime", "harmony": true },
    "deviceAttestation":  { "ios": true,  "miniprogram": false,     "android": false, "harmony": false }
  }
}
```

`contract/contract.json` 先写占位 sha（Step 5 由 gen 刷新前先手算）：

```json
{ "version": 1, "files": { "transfer-state.json": "", "strings.json": "", "tokens.json": "", "capabilities.json": "" } }
```

- [ ] **Step 4: 写 `contract/tools/lib.mjs` 的加载部分**

```js
// contract/tools/lib.mjs — 契约加载与四端渲染。gen.mjs 与 check.mjs 共用。
import { readFileSync } from 'node:fs'
import { join } from 'node:path'
import { createHash } from 'node:crypto'

export const DATA_FILES = ['transfer-state.json', 'strings.json', 'tokens.json', 'capabilities.json']
export const HEADER = 'GENERATED by contract/tools/gen.mjs from contract/*.json — 不要手改，改 contract/ 再跑 gen'

export function sha256(text) {
  return createHash('sha256').update(text).digest('hex')
}

function readJson(root, name) {
  return JSON.parse(readFileSync(join(root, 'contract', name), 'utf8'))
}

export function loadContract(root) {
  return {
    root,
    state: readJson(root, 'transfer-state.json'),
    strings: readJson(root, 'strings.json'),
    tokens: readJson(root, 'tokens.json'),
    caps: readJson(root, 'capabilities.json'),
    manifest: readJson(root, 'contract.json'),
  }
}

/** 重新计算 contract.json 应有的内容 */
export function manifestFor(root, version) {
  const files = {}
  for (const f of DATA_FILES) files[f] = sha256(readFileSync(join(root, 'contract', f), 'utf8'))
  return { version, files }
}
```

- [ ] **Step 5: 用一行脚本填入 sha**

Run:
```bash
node -e "import('./contract/tools/lib.mjs').then(m=>{const fs=require('fs');fs.writeFileSync('contract/contract.json',JSON.stringify(m.manifestFor(process.cwd(),1),null,2)+'\n')})"
```

- [ ] **Step 6: 跑测试确认通过**

Run: `node --test tests/contract-tools.test.ts`
Expected: 2 passed

- [ ] **Step 7: 提交**

```bash
git add contract tests/contract-tools.test.ts
git commit -m "feat(contract): 四端契约数据文件与 sha 清单（状态机/文案/令牌/能力）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: 夹具与 schema 校验

**Files:**
- Create: `contract/fixtures/{tally,transitions,status-merge,restore,delete-warning}.json`
- Create: `contract/schema/{tally,transitions,status-merge,restore,delete-warning}.schema.json`
- Create: `contract/tools/check.mjs`（本任务实现 ① sha、② schema + 引用）
- Modify: `package.json`（加 ajv devDependency 与脚本）
- Test: `tests/contract-tools.test.ts`

**Interfaces:**
- Produces: `check.mjs` 退出码 0/1，参数 `--root DIR`、`--quick`；导出 `runChecks(root, { quick }) → { ok, problems: string[] }`
- 夹具格式（四端适配层依赖）：
  - `tally.json`: `{ cases: [{ name, states: State[], expect: { uploading, failed, staged, landed, total } }] }`
  - `transitions.json`: `{ cases: [{ from, event, attempts, to: State | null }] }`（`null` = 非法迁移，实现应拒绝）
  - `status-merge.json`: `{ cases: [{ name, state, status: { delivered, waitingSeconds, expiresAt? }, expect: { state, waitingSeconds: number|null, expiresAt: string|null } }] }`
  - `restore.json`: `{ cases: [{ state, attempts, expect: State }] }`
  - `delete-warning.json`: `{ cases: [{ states: State[], expect: { level: 'unsent'|'staged'|'landed', n } }] }`

- [ ] **Step 1: 装 ajv 并加脚本**

Run: `npm i -D ajv@^8`

`package.json` scripts 加：

```json
"contract:gen": "node contract/tools/gen.mjs",
"contract:check": "node contract/tools/check.mjs"
```

- [ ] **Step 2: 写夹具**

`contract/fixtures/tally.json`：

```json
{ "cases": [
  { "name": "mixed", "states": ["waiting", "failed", "uploading", "uploaded", "arrived", "arrived"],
    "expect": { "uploading": 3, "failed": 1, "staged": 1, "landed": 2, "total": 6 } },
  { "name": "empty", "states": [], "expect": { "uploading": 0, "failed": 0, "staged": 0, "landed": 0, "total": 0 } },
  { "name": "all failed", "states": ["failed", "failed"], "expect": { "uploading": 2, "failed": 2, "staged": 0, "landed": 0, "total": 2 } },
  { "name": "only staged", "states": ["uploaded", "uploaded", "uploaded"], "expect": { "uploading": 0, "failed": 0, "staged": 3, "landed": 0, "total": 3 } }
] }
```

`contract/fixtures/transitions.json`：

```json
{ "cases": [
  { "from": "waiting",   "event": "kick",             "attempts": 0, "to": "uploading" },
  { "from": "uploading", "event": "http_2xx",         "attempts": 0, "to": "uploaded" },
  { "from": "uploading", "event": "http_error",       "attempts": 0, "to": "failed" },
  { "from": "uploading", "event": "network_error",    "attempts": 1, "to": "failed" },
  { "from": "failed",    "event": "retry_manual",     "attempts": 9, "to": "waiting" },
  { "from": "failed",    "event": "retry_auto",       "attempts": 3, "to": "waiting" },
  { "from": "failed",    "event": "retry_auto",       "attempts": 4, "to": "failed" },
  { "from": "uploaded",  "event": "status_delivered", "attempts": 0, "to": "arrived" },
  { "from": "uploaded",  "event": "status_pending",   "attempts": 0, "to": "uploaded" },
  { "from": "uploading", "event": "app_launch",       "attempts": 0, "to": "waiting" },
  { "from": "failed",    "event": "app_launch",       "attempts": 2, "to": "waiting" },
  { "from": "failed",    "event": "app_launch",       "attempts": 4, "to": "failed" },
  { "from": "arrived",   "event": "kick",             "attempts": 0, "to": null },
  { "from": "waiting",   "event": "http_2xx",         "attempts": 0, "to": null },
  { "from": "uploaded",  "event": "kick",             "attempts": 0, "to": null },
  { "from": "arrived",   "event": "status_delivered", "attempts": 0, "to": null }
] }
```

`contract/fixtures/status-merge.json`：

```json
{ "cases": [
  { "name": "delivered clears waiting fields", "state": "uploaded",
    "status": { "delivered": true, "waitingSeconds": 0 },
    "expect": { "state": "arrived", "waitingSeconds": null, "expiresAt": null } },
  { "name": "pending backfills", "state": "uploaded",
    "status": { "delivered": false, "waitingSeconds": 120, "expiresAt": "2026-09-09T10:00:00" },
    "expect": { "state": "uploaded", "waitingSeconds": 120, "expiresAt": "2026-09-09T10:00:00" } },
  { "name": "pending without expiresAt", "state": "uploaded",
    "status": { "delivered": false, "waitingSeconds": 5 },
    "expect": { "state": "uploaded", "waitingSeconds": 5, "expiresAt": null } },
  { "name": "non-uploaded ignores status", "state": "waiting",
    "status": { "delivered": true, "waitingSeconds": 0 },
    "expect": { "state": "waiting", "waitingSeconds": null, "expiresAt": null } },
  { "name": "arrived stays arrived", "state": "arrived",
    "status": { "delivered": false, "waitingSeconds": 30 },
    "expect": { "state": "arrived", "waitingSeconds": null, "expiresAt": null } }
] }
```

`contract/fixtures/restore.json`：

```json
{ "cases": [
  { "state": "uploading", "attempts": 0, "expect": "waiting" },
  { "state": "failed",    "attempts": 2, "expect": "waiting" },
  { "state": "failed",    "attempts": 3, "expect": "waiting" },
  { "state": "failed",    "attempts": 4, "expect": "failed" },
  { "state": "uploaded",  "attempts": 0, "expect": "uploaded" },
  { "state": "waiting",   "attempts": 0, "expect": "waiting" },
  { "state": "arrived",   "attempts": 0, "expect": "arrived" }
] }
```

`contract/fixtures/delete-warning.json`：

```json
{ "cases": [
  { "states": ["arrived", "arrived"],            "expect": { "level": "landed", "n": 2 } },
  { "states": ["arrived", "uploaded"],           "expect": { "level": "staged", "n": 1 } },
  { "states": ["uploaded", "failed", "waiting"], "expect": { "level": "unsent", "n": 2 } },
  { "states": ["uploading"],                     "expect": { "level": "unsent", "n": 1 } },
  { "states": [],                                "expect": { "level": "landed", "n": 0 } }
] }
```

- [ ] **Step 3: 写五个 schema**

`contract/schema/tally.schema.json`：

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object", "required": ["cases"], "additionalProperties": false,
  "properties": { "cases": { "type": "array", "minItems": 1, "items": {
    "type": "object", "required": ["name", "states", "expect"], "additionalProperties": false,
    "properties": {
      "name": { "type": "string" },
      "states": { "type": "array", "items": { "$ref": "#/$defs/state" } },
      "expect": { "type": "object", "required": ["uploading", "failed", "staged", "landed", "total"], "additionalProperties": false,
        "properties": { "uploading": { "type": "integer" }, "failed": { "type": "integer" }, "staged": { "type": "integer" },
                        "landed": { "type": "integer" }, "total": { "type": "integer" } } }
    } } } },
  "$defs": { "state": { "enum": ["waiting", "uploading", "uploaded", "arrived", "failed"] } }
}
```

`contract/schema/transitions.schema.json`：

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object", "required": ["cases"], "additionalProperties": false,
  "properties": { "cases": { "type": "array", "minItems": 1, "items": {
    "type": "object", "required": ["from", "event", "attempts", "to"], "additionalProperties": false,
    "properties": {
      "from": { "$ref": "#/$defs/state" },
      "event": { "enum": ["kick", "http_2xx", "http_error", "network_error", "retry_manual", "retry_auto", "status_delivered", "status_pending", "app_launch"] },
      "attempts": { "type": "integer", "minimum": 0 },
      "to": { "oneOf": [{ "$ref": "#/$defs/state" }, { "type": "null" }] }
    } } } },
  "$defs": { "state": { "enum": ["waiting", "uploading", "uploaded", "arrived", "failed"] } }
}
```

`contract/schema/status-merge.schema.json`：

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object", "required": ["cases"], "additionalProperties": false,
  "properties": { "cases": { "type": "array", "minItems": 1, "items": {
    "type": "object", "required": ["name", "state", "status", "expect"], "additionalProperties": false,
    "properties": {
      "name": { "type": "string" },
      "state": { "$ref": "#/$defs/state" },
      "status": { "type": "object", "required": ["delivered", "waitingSeconds"], "additionalProperties": false,
        "properties": { "delivered": { "type": "boolean" }, "waitingSeconds": { "type": "integer" }, "expiresAt": { "type": "string" } } },
      "expect": { "type": "object", "required": ["state", "waitingSeconds", "expiresAt"], "additionalProperties": false,
        "properties": { "state": { "$ref": "#/$defs/state" },
                        "waitingSeconds": { "type": ["integer", "null"] }, "expiresAt": { "type": ["string", "null"] } } }
    } } } },
  "$defs": { "state": { "enum": ["waiting", "uploading", "uploaded", "arrived", "failed"] } }
}
```

`contract/schema/restore.schema.json`：

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object", "required": ["cases"], "additionalProperties": false,
  "properties": { "cases": { "type": "array", "minItems": 1, "items": {
    "type": "object", "required": ["state", "attempts", "expect"], "additionalProperties": false,
    "properties": { "state": { "$ref": "#/$defs/state" }, "attempts": { "type": "integer", "minimum": 0 }, "expect": { "$ref": "#/$defs/state" } }
  } } },
  "$defs": { "state": { "enum": ["waiting", "uploading", "uploaded", "arrived", "failed"] } }
}
```

`contract/schema/delete-warning.schema.json`：

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object", "required": ["cases"], "additionalProperties": false,
  "properties": { "cases": { "type": "array", "minItems": 1, "items": {
    "type": "object", "required": ["states", "expect"], "additionalProperties": false,
    "properties": {
      "states": { "type": "array", "items": { "$ref": "#/$defs/state" } },
      "expect": { "type": "object", "required": ["level", "n"], "additionalProperties": false,
        "properties": { "level": { "enum": ["unsent", "staged", "landed"] }, "n": { "type": "integer", "minimum": 0 } } }
    } } } },
  "$defs": { "state": { "enum": ["waiting", "uploading", "uploaded", "arrived", "failed"] } }
}
```

- [ ] **Step 4: 写失败测试**

追加到 `tests/contract-tools.test.ts`：

```ts
import { mkdtempSync, cpSync, writeFileSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { runChecks } from '../contract/tools/check.mjs'

function tempCopy(): string {
  const dir = mkdtempSync(join(tmpdir(), 'awd-contract-'))
  cpSync(join(ROOT, 'contract'), join(dir, 'contract'), { recursive: true })
  return dir
}

test('check：现状全绿（quick）', () => {
  const r = runChecks(ROOT, { quick: true })
  assert.deepEqual(r.problems, [])
  assert.equal(r.ok, true)
})

test('check：数据文件改了没跑 gen → sha 不一致要红', () => {
  const dir = tempCopy()
  const p = join(dir, 'contract', 'strings.json')
  const s = JSON.parse(readFileSync(p, 'utf8'))
  s['phase.landed']['zh-Hans'] = '已归档'
  writeFileSync(p, JSON.stringify(s, null, 2))
  const r = runChecks(dir, { quick: true })
  assert.ok(r.problems.some((m) => m.includes('strings.json') && m.includes('sha')), r.problems.join('\n'))
})

test('check：坏夹具要红', () => {
  const dir = tempCopy()
  const p = join(dir, 'contract', 'fixtures', 'tally.json')
  writeFileSync(p, JSON.stringify({ cases: [{ name: 'x', states: ['flying'], expect: { uploading: 0, failed: 0, staged: 0, landed: 0, total: 0 } }] }))
  const r = runChecks(dir, { quick: true })
  assert.ok(r.problems.some((m) => m.includes('fixtures/tally.json')), r.problems.join('\n'))
})

test('check：夹具期望必须与迁移表一致', () => {
  const dir = tempCopy()
  const p = join(dir, 'contract', 'fixtures', 'transitions.json')
  const f = JSON.parse(readFileSync(p, 'utf8'))
  f.cases[0].to = 'arrived'  // waiting + kick 应为 uploading
  writeFileSync(p, JSON.stringify(f))
  const r = runChecks(dir, { quick: true })
  assert.ok(r.problems.some((m) => m.includes('transitions.json') && m.includes('迁移表')), r.problems.join('\n'))
})
```

- [ ] **Step 5: 跑测试确认失败**

Run: `node --test tests/contract-tools.test.ts`
Expected: FAIL，`Cannot find module '../contract/tools/check.mjs'`

- [ ] **Step 6: 写 `check.mjs`（① ②）**

```js
#!/usr/bin/env node
// contract/tools/check.mjs — 契约校验。① 清单 sha ② 夹具 schema + 与迁移表一致 ③ 生成物未过期 ④ API 副本未漂 ⑤ 无内联文案
// 用法：node contract/tools/check.mjs [--root DIR] [--quick]   （--quick 跳过 ⑤）
import { readFileSync, existsSync, readdirSync } from 'node:fs'
import { join, dirname, resolve } from 'node:path'
import { fileURLToPath } from 'node:url'
import Ajv2020 from 'ajv/dist/2020.js'
import { loadContract, manifestFor, DATA_FILES } from './lib.mjs'

const FIXTURES = ['tally', 'transitions', 'status-merge', 'restore', 'delete-warning']

function evalGuard(guard, attempts, state) {
  if (!guard) return true
  if (guard === 'attempts <= maxAutoRetries') return attempts <= state.maxAutoRetries
  throw new Error(`未知 guard: ${guard}`)
}

/** 参考实现：只在校验器里用，四端各自手写同语义的函数并用夹具对拍 */
export function referenceNext(state, from, event, attempts) {
  const rules = state.transitions.filter((t) => t.from === from && t.event === event)
  if (rules.length === 0) return null
  for (const r of rules) if (evalGuard(r.guard, attempts, state)) return r.to
  return from
}

function checkManifest(c, problems) {
  const expected = manifestFor(c.root, c.manifest.version)
  for (const f of DATA_FILES) {
    if (c.manifest.files[f] !== expected.files[f]) problems.push(`contract.json 里 ${f} 的 sha 过期：改了数据没跑 gen`)
  }
}

function checkFixtures(c, problems) {
  const ajv = new Ajv2020({ allErrors: true })
  for (const name of FIXTURES) {
    const schema = JSON.parse(readFileSync(join(c.root, 'contract', 'schema', `${name}.schema.json`), 'utf8'))
    const data = JSON.parse(readFileSync(join(c.root, 'contract', 'fixtures', `${name}.json`), 'utf8'))
    const validate = ajv.compile(schema)
    if (!validate(data)) {
      for (const e of validate.errors) problems.push(`fixtures/${name}.json 不合 schema：${e.instancePath} ${e.message}`)
    }
  }
  // 迁移夹具的期望必须由迁移表推出，夹具不能自说自话
  const tr = JSON.parse(readFileSync(join(c.root, 'contract', 'fixtures', 'transitions.json'), 'utf8'))
  for (const k of tr.cases) {
    const got = referenceNext(c.state, k.from, k.event, k.attempts)
    if (got !== k.to) problems.push(`fixtures/transitions.json：${k.from}+${k.event}(attempts=${k.attempts}) 期望 ${k.to}，迁移表推出 ${got}`)
  }
  const rs = JSON.parse(readFileSync(join(c.root, 'contract', 'fixtures', 'restore.json'), 'utf8'))
  for (const k of rs.cases) {
    const got = referenceNext(c.state, k.state, 'app_launch', k.attempts) ?? k.state
    if (got !== k.expect) problems.push(`fixtures/restore.json：${k.state}(attempts=${k.attempts}) 期望 ${k.expect}，迁移表推出 ${got}`)
  }
}

export function runChecks(root, { quick = false } = {}) {
  const problems = []
  const c = loadContract(root)
  checkManifest(c, problems)
  checkFixtures(c, problems)
  // ③ ④ ⑤ 在后续任务补入
  return { ok: problems.length === 0, problems }
}

if (process.argv[1] && resolve(process.argv[1]) === fileURLToPath(import.meta.url)) {
  const args = process.argv.slice(2)
  const i = args.indexOf('--root')
  const root = resolve(i >= 0 ? args[i + 1] : join(dirname(fileURLToPath(import.meta.url)), '..', '..'))
  const r = runChecks(root, { quick: args.includes('--quick') })
  if (!r.ok) { for (const p of r.problems) console.error('✗', p); process.exit(1) }
  console.log('契约校验通过')
}
```

- [ ] **Step 7: 跑测试确认通过**

Run: `node --test tests/contract-tools.test.ts && node contract/tools/check.mjs --quick`
Expected: 6 passed；`契约校验通过`

- [ ] **Step 8: 提交**

```bash
git add contract package.json package-lock.json tests/contract-tools.test.ts
git commit -m "feat(contract): 五个黄金夹具 + schema，check.mjs 校验清单与夹具

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: 生成器与生成物过期检测

**Files:**
- Modify: `contract/tools/lib.mjs`（加 `outputs(c)` 与各渲染器）
- Create: `contract/tools/gen.mjs`、`contract/tools/wxss-footer.css`
- Modify: `contract/tools/check.mjs`（加 ③）
- Create（由 gen 产出）: `miniprogram/styles/tokens.wxss`（覆盖）、`miniprogram/utils/contract/{states,strings,capabilities}.ts`、`ios/Sources/Contract/{Tokens,Strings,States,Capabilities}.swift`、`android/contract/src/main/kotlin/com/aiworkdeck/contract/{Tokens,Strings,States,Capabilities}.kt`、`harmony/contract/src/main/ets/{Tokens,Strings,States,Capabilities}.ets`
- Test: `tests/contract-tools.test.ts`

**Interfaces:**
- Produces（TS，`miniprogram/utils/contract/states.ts`）：`CONTRACT_VERSION: number`、`type QueueState`、`STATES`、`STATE_ALIASES: Record<string, QueueState>`、`type Phase`、`PHASE_OF: Record<QueueState, Phase>`、`PHASE_LABEL_KEY: Record<Phase, string>`、`PHASE_DOT: Record<Phase, string>`、`FAILED_DOT: string`、`STATE_TEXT_KEY: Record<QueueState, string>`、`WHERE_KEY: Record<QueueState, string>`、`RETRY_DELAYS_MS: number[]`、`MAX_AUTO_RETRIES: number`、`type TransferEvent`、`TRANSITIONS: ReadonlyArray<{ from: QueueState; event: TransferEvent; to: QueueState; guard?: string }>`、`DELETE_WARN_ORDER: Phase[]`、`DELETE_WARN_LEVEL: Record<Phase, 'unsent'|'staged'|'landed'>`、`DELETE_WARN_KEY: Record<'unsent'|'staged'|'landed', string>`
- Produces（TS，`strings.ts`）：`STRINGS: Record<string, { 'zh-Hans': string; en: string }>`
- Produces（TS，`capabilities.ts`）：`CAPS: { backgroundUpload: boolean; maxVideoSeconds: number | null; continuousSegments: boolean; glassBlur: boolean | 'runtime'; deviceAttestation: boolean }`、`DEGRADED_NOTICE: Record<string, string>`
- Produces（Swift，`ContractStates`）：`static let version: Int`、`states: [String]`、`aliases: [String: String]`、`phaseOf: [String: String]`、`phaseLabelKey: [String: String]`、`phaseDot: [String: String]`、`failedDot: String`、`stateTextKey`、`whereKey`、`retryDelaysMs: [Int]`、`maxAutoRetries: Int`、`transitions: [(from: String, event: String, to: String, guard: String?)]`、`deleteWarnOrder: [String]`、`deleteWarnLevel: [String: String]`、`deleteWarnKey: [String: String]`
- Produces（Swift，`ContractStrings.table: [String: [String: String]]`；`ContractCapabilities` 常量；`T.L/T.D/T.S/T.Sp/T.Ty/T.touchMin/T.Motion`）
- Produces（Kotlin `object ContractStates/ContractStrings/ContractCapabilities/T`，ArkTS 同名 `export const`）

- [ ] **Step 1: 写失败测试**

追加到 `tests/contract-tools.test.ts`：

```ts
import { outputs } from '../contract/tools/lib.mjs'
import { existsSync } from 'node:fs'

test('gen：每个生成物都带 GENERATED 头且落在约定路径', () => {
  const c = loadContract(ROOT)
  const files = outputs(c)
  const expected = [
    'miniprogram/styles/tokens.wxss',
    'miniprogram/utils/contract/states.ts', 'miniprogram/utils/contract/strings.ts', 'miniprogram/utils/contract/capabilities.ts',
    'ios/Sources/Contract/Tokens.swift', 'ios/Sources/Contract/Strings.swift', 'ios/Sources/Contract/States.swift', 'ios/Sources/Contract/Capabilities.swift',
    'android/contract/src/main/kotlin/com/aiworkdeck/contract/Tokens.kt', 'android/contract/src/main/kotlin/com/aiworkdeck/contract/Strings.kt',
    'android/contract/src/main/kotlin/com/aiworkdeck/contract/States.kt', 'android/contract/src/main/kotlin/com/aiworkdeck/contract/Capabilities.kt',
    'harmony/contract/src/main/ets/Tokens.ets', 'harmony/contract/src/main/ets/Strings.ets', 'harmony/contract/src/main/ets/States.ets', 'harmony/contract/src/main/ets/Capabilities.ets',
  ]
  assert.deepEqual([...files.keys()].sort(), expected.sort())
  for (const [p, content] of files) assert.ok(content.includes('GENERATED by contract/tools/gen.mjs'), `${p} 缺生成头`)
})

test('gen：wxss 间距 ×2 出 rpx，字号 px，旧变量名保留', () => {
  const wxss = outputs(loadContract(ROOT)).get('miniprogram/styles/tokens.wxss')!
  assert.match(wxss, /--s4: 32rpx;/)
  assert.match(wxss, /--gutter: 40rpx;/)
  assert.match(wxss, /--t-body: 15px;/)
  assert.match(wxss, /--st-moving: #1E3A8A;/)
  assert.match(wxss, /--dk-rule: rgba\(255, 255, 255, 0\.10\);/)
  assert.match(wxss, /--touch-min: 88rpx;/)
  assert.match(wxss, /\.label \{/)  // footer 工具类保留
})

test('gen：Swift 令牌保留 T.L/T.D/T.S/T.Sp 调用面', () => {
  const swift = outputs(loadContract(ROOT)).get('ios/Sources/Contract/Tokens.swift')!
  assert.match(swift, /enum T \{/)
  assert.match(swift, /static let accent = Color\(hex: 0x1E3A8A\)/)
  assert.match(swift, /static let rule = Color\(\.sRGB, red: 1\.000, green: 1\.000, blue: 1\.000, opacity: 0\.10\)/)
  assert.match(swift, /static let gutter: CGFloat = 20/)
  assert.match(swift, /static let body: CGFloat = 15/)
  assert.match(swift, /static let touchMin: CGFloat = 44/)
})

test('check：生成物被手改要红', () => {
  const dir = tempCopy()
  // 把真实生成物拷进临时根，再改坏一个
  for (const rel of outputs(loadContract(ROOT)).keys()) {
    const src = join(ROOT, rel)
    if (existsSync(src)) cpSync(src, join(dir, rel))
  }
  cpSync(join(ROOT, 'contract', 'tools', 'wxss-footer.css'), join(dir, 'contract', 'tools', 'wxss-footer.css'))
  const p = join(dir, 'miniprogram', 'utils', 'contract', 'strings.ts')
  writeFileSync(p, readFileSync(p, 'utf8').replace('已落盘', '已归档'))
  const r = runChecks(dir, { quick: true })
  assert.ok(r.problems.some((m) => m.includes('miniprogram/utils/contract/strings.ts') && m.includes('过期')), r.problems.join('\n'))
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/contract-tools.test.ts`
Expected: FAIL，`outputs is not a function` 或缺路径

- [ ] **Step 3: 写 footer 与渲染器**

`contract/tools/wxss-footer.css`（从现 tokens.wxss 尾部原样搬入）：

```css
/* 全大写小标签。字距拉开是这套排版的签名，别改。 */
.label {
  font-size: var(--t-nano);
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--fg-faint);
  font-weight: 500;
}

.mono {
  font-family: var(--font-mono);
  font-variant-numeric: tabular-nums;
}

.rule {
  height: 1rpx;
  background: var(--rule);
}
```

追加到 `contract/tools/lib.mjs`：

```js
const H = (comment) => `${comment} ${HEADER}\n`
const q = (s) => JSON.stringify(s)

function parseColor(v) {
  const m = /^rgba\((\d+),\s*(\d+),\s*(\d+),\s*([\d.]+)\)$/.exec(v)
  if (m) return { r: +m[1], g: +m[2], b: +m[3], a: +m[4] }
  const hex = v.replace('#', '')
  return { r: parseInt(hex.slice(0, 2), 16), g: parseInt(hex.slice(2, 4), 16), b: parseInt(hex.slice(4, 6), 16), a: 1, hex }
}
const swiftColor = (v) => {
  const c = parseColor(v)
  if (c.hex) return `Color(hex: 0x${c.hex.toUpperCase()})`
  const f = (n) => (n / 255).toFixed(3)
  return `Color(.sRGB, red: ${f(c.r)}, green: ${f(c.g)}, blue: ${f(c.b)}, opacity: ${c.a.toFixed(2)})`
}
const kotlinColor = (v) => {
  const c = parseColor(v)
  const a = Math.round(c.a * 255)
  const hex = [a, c.r, c.g, c.b].map((n) => n.toString(16).padStart(2, '0')).join('').toUpperCase()
  return `0x${hex}L`
}

// ---- wxss ----
function renderWxss(c) {
  const t = c.tokens, n = t.wxssNames, f = t.unit.wxssSpacingFactor
  const lines = [`/* ${HEADER} */`, 'page {']
  for (const grp of ['L', 'D', 'S']) for (const [k, v] of Object.entries(t[grp])) lines.push(`  --${n[grp][k]}: ${v};`)
  for (const [k, v] of Object.entries(t.Sp)) lines.push(`  --${k === 'gutter' ? 'gutter' : k}: ${v * f}rpx;`)
  for (const [k, v] of Object.entries(t.Ty)) lines.push(`  --t-${k}: ${v}${t.unit.typeUnit};`)
  lines.push(`  --font-sans: ${t.font.sans};`, `  --font-mono: ${t.font.mono};`)
  lines.push(`  --d-fast: ${t.motion.fastMs}ms;`, `  --d-base: ${t.motion.baseMs}ms;`, `  --d-slow: ${t.motion.slowMs}ms;`)
  lines.push(`  --e-out: ${t.motion.easeOut};`, `  --e-in: ${t.motion.easeIn};`)
  lines.push(`  --touch-min: ${t.touchMin * f}rpx;`, '}', '')
  const footer = readFileSync(join(c.root, 'contract', 'tools', 'wxss-footer.css'), 'utf8')
  return lines.join('\n') + '\n' + footer
}

// ---- TS ----
function renderTsStates(c) {
  const s = c.state
  const union = (arr) => arr.map(q).join(' | ')
  return H('//') + `export const CONTRACT_VERSION = ${s.version}
export type QueueState = ${union(s.states)}
export const STATES: readonly QueueState[] = ${q(s.states)}
export const STATE_ALIASES: Record<string, QueueState> = ${q(s.aliases)}
export type Phase = ${union(Object.keys(s.phases))}
export const PHASE_OF: Record<QueueState, Phase> = ${q(Object.fromEntries(Object.entries(s.phases).flatMap(([p, v]) => v.states.map((st) => [st, p]))))}
export const PHASE_LABEL_KEY: Record<Phase, string> = ${q(Object.fromEntries(Object.entries(s.phases).map(([p, v]) => [p, v.label])))}
export const PHASE_DOT: Record<Phase, string> = ${q(Object.fromEntries(Object.entries(s.phases).map(([p, v]) => [p, v.dot])))}
export const FAILED_DOT = ${q(s.failedDot)}
export const STATE_TEXT_KEY: Record<QueueState, string> = ${q(s.stateText)}
export const WHERE_KEY: Record<QueueState, string> = ${q(s.whereItIs)}
export const RETRY_DELAYS_MS: readonly number[] = ${q(s.retryDelaysMs)}
export const MAX_AUTO_RETRIES = ${s.maxAutoRetries}
export type TransferEvent = ${union(s.events)}
export const TRANSITIONS: ReadonlyArray<{ from: QueueState; event: TransferEvent; to: QueueState; guard?: string }> = ${JSON.stringify(s.transitions, null, 2)}
export const DELETE_WARN_ORDER: readonly Phase[] = ${q(s.deleteWarning.order)}
export const DELETE_WARN_LEVEL: Record<Phase, 'unsent' | 'staged' | 'landed'> = ${q(s.deleteWarning.levels)}
export const DELETE_WARN_KEY: Record<'unsent' | 'staged' | 'landed', string> = ${q(s.deleteWarning.keys)}
`
}
function renderTsStrings(c) {
  return H('//') + `export const STRINGS: Record<string, { 'zh-Hans': string; en: string }> = ${JSON.stringify(c.strings, null, 2)}\n`
}
function renderTsCaps(c, client) {
  const caps = Object.fromEntries(Object.entries(c.caps.capabilities).map(([k, v]) => [k, v[client]]))
  const notice = Object.fromEntries(Object.entries(c.caps.capabilities).filter(([, v]) => v.degradedNotice).map(([k, v]) => [k, v.degradedNotice]))
  return H('//') + `export const CLIENT = ${q(client)}
export const CAPS = ${JSON.stringify(caps, null, 2)} as const
export const DEGRADED_NOTICE: Record<string, string> = ${q(notice)}
`
}

// ---- Swift ----
function renderSwiftTokens(c) {
  const t = c.tokens
  const grp = (name, obj, fn) => `    enum ${name} {\n${Object.entries(obj).map(([k, v]) => `        static let ${k} = ${fn(v)}`).join('\n')}\n    }\n`
  const num = (name, obj) => `    enum ${name} {\n${Object.entries(obj).map(([k, v]) => `        static let ${k}: CGFloat = ${v}`).join('\n')}\n    }\n`
  return H('//') + `import SwiftUI

/// 设计令牌。F（字体）与 A（动效）在 Design/Typography.swift 里以 extension 补齐。
enum T {
${grp('L', t.L, swiftColor)}
${grp('D', t.D, swiftColor)}
${grp('S', t.S, swiftColor)}
${num('Sp', t.Sp)}
${num('Ty', t.Ty)}
    enum Motion {
        static let fast: Double = ${(t.motion.fastMs / 1000).toFixed(2)}
        static let base: Double = ${(t.motion.baseMs / 1000).toFixed(2)}
        static let slow: Double = ${(t.motion.slowMs / 1000).toFixed(2)}
    }
    static let touchMin: CGFloat = ${t.touchMin}
    static let contractVersion = ${t.version}
}
`
}
const swiftDict = (obj) => `[${Object.entries(obj).map(([k, v]) => `${q(k)}: ${q(v)}`).join(', ')}]`
function renderSwiftStrings(c) {
  const rows = Object.entries(c.strings).map(([k, v]) => `        ${q(k)}: [${q('zh-Hans')}: ${q(v['zh-Hans'])}, ${q('en')}: ${q(v.en)}],`).join('\n')
  return H('//') + `enum ContractStrings {\n    static let table: [String: [String: String]] = [\n${rows}\n    ]\n}\n`
}
function renderSwiftStates(c) {
  const s = c.state
  const tr = s.transitions.map((t) => `        (from: ${q(t.from)}, event: ${q(t.event)}, to: ${q(t.to)}, guard: ${t.guard ? q(t.guard) : 'nil'}),`).join('\n')
  return H('//') + `enum ContractStates {
    static let version = ${s.version}
    static let states: [String] = ${q(s.states)}
    static let aliases: [String: String] = ${swiftDict(s.aliases)}
    static let phaseOf: [String: String] = ${swiftDict(Object.fromEntries(Object.entries(s.phases).flatMap(([p, v]) => v.states.map((st) => [st, p]))))}
    static let phaseLabelKey: [String: String] = ${swiftDict(Object.fromEntries(Object.entries(s.phases).map(([p, v]) => [p, v.label])))}
    static let phaseDot: [String: String] = ${swiftDict(Object.fromEntries(Object.entries(s.phases).map(([p, v]) => [p, v.dot])))}
    static let failedDot = ${q(s.failedDot)}
    static let stateTextKey: [String: String] = ${swiftDict(s.stateText)}
    static let whereKey: [String: String] = ${swiftDict(s.whereItIs)}
    static let retryDelaysMs: [Int] = ${q(s.retryDelaysMs)}
    static let maxAutoRetries = ${s.maxAutoRetries}
    static let events: [String] = ${q(s.events)}
    static let transitions: [(from: String, event: String, to: String, guard: String?)] = [
${tr}
    ]
    static let deleteWarnOrder: [String] = ${q(s.deleteWarning.order)}
    static let deleteWarnLevel: [String: String] = ${swiftDict(s.deleteWarning.levels)}
    static let deleteWarnKey: [String: String] = ${swiftDict(s.deleteWarning.keys)}
}
`
}
function renderSwiftCaps(c) {
  const v = (x) => x === null ? 'nil' : typeof x === 'string' ? q(x) : String(x)
  const caps = c.caps.capabilities
  return H('//') + `enum ContractCapabilities {
    static let client = "ios"
    static let backgroundUpload: Bool = ${caps.backgroundUpload.ios}
    static let maxVideoSeconds: Int? = ${v(caps.maxVideoSeconds.ios)}
    static let continuousSegments: Bool = ${caps.continuousSegments.ios}
    /// true / false，或 "runtime" 表示运行时探测
    static let glassBlur: String = ${q(String(caps.glassBlur.ios))}
    static let deviceAttestation: Bool = ${caps.deviceAttestation.ios}
    static let degradedNotice: [String: String] = ${swiftDict(Object.fromEntries(Object.entries(caps).filter(([, x]) => x.degradedNotice).map(([k, x]) => [k, x.degradedNotice])))}
}
`
}

// ---- Kotlin ----
const ktMap = (obj) => `mapOf(${Object.entries(obj).map(([k, v]) => `${q(k)} to ${q(v)}`).join(', ')})`
function renderKtTokens(c) {
  const t = c.tokens
  const grp = (name, obj, fn) => `    object ${name} {\n${Object.entries(obj).map(([k, v]) => `        const val ${k}: Long = ${fn(v)}`).join('\n')}\n    }`
  const num = (name, obj) => `    object ${name} {\n${Object.entries(obj).map(([k, v]) => `        const val ${k}: Int = ${v}`).join('\n')}\n    }`
  return H('//') + `package com.aiworkdeck.contract

/** 颜色为 ARGB Long；间距单位 dp，字号单位 sp。 */
object T {
${grp('L', t.L, kotlinColor)}
${grp('D', t.D, kotlinColor)}
${grp('S', t.S, kotlinColor)}
${num('Sp', t.Sp)}
${num('Ty', t.Ty)}
    object Motion { const val fastMs: Int = ${t.motion.fastMs}; const val baseMs: Int = ${t.motion.baseMs}; const val slowMs: Int = ${t.motion.slowMs} }
    const val touchMin: Int = ${t.touchMin}
    const val contractVersion: Int = ${t.version}
}
`
}
function renderKtStrings(c) {
  const rows = Object.entries(c.strings).map(([k, v]) => `        ${q(k)} to mapOf("zh-Hans" to ${q(v['zh-Hans'])}, "en" to ${q(v.en)}),`).join('\n')
  return H('//') + `package com.aiworkdeck.contract\n\nobject ContractStrings {\n    val table: Map<String, Map<String, String>> = mapOf(\n${rows}\n    )\n}\n`
}
function renderKtStates(c) {
  const s = c.state
  const tr = s.transitions.map((t) => `        Transition(${q(t.from)}, ${q(t.event)}, ${q(t.to)}, ${t.guard ? q(t.guard) : 'null'}),`).join('\n')
  return H('//') + `package com.aiworkdeck.contract

data class Transition(val from: String, val event: String, val to: String, val guard: String?)

object ContractStates {
    const val version: Int = ${s.version}
    val states: List<String> = listOf(${s.states.map(q).join(', ')})
    val aliases: Map<String, String> = ${ktMap(s.aliases)}
    val phaseOf: Map<String, String> = ${ktMap(Object.fromEntries(Object.entries(s.phases).flatMap(([p, v]) => v.states.map((st) => [st, p]))))}
    val phaseLabelKey: Map<String, String> = ${ktMap(Object.fromEntries(Object.entries(s.phases).map(([p, v]) => [p, v.label])))}
    val phaseDot: Map<String, String> = ${ktMap(Object.fromEntries(Object.entries(s.phases).map(([p, v]) => [p, v.dot])))}
    const val failedDot: String = ${q(s.failedDot)}
    val stateTextKey: Map<String, String> = ${ktMap(s.stateText)}
    val whereKey: Map<String, String> = ${ktMap(s.whereItIs)}
    val retryDelaysMs: List<Int> = listOf(${s.retryDelaysMs.join(', ')})
    const val maxAutoRetries: Int = ${s.maxAutoRetries}
    val events: List<String> = listOf(${s.events.map(q).join(', ')})
    val transitions: List<Transition> = listOf(
${tr}
    )
    val deleteWarnOrder: List<String> = listOf(${s.deleteWarning.order.map(q).join(', ')})
    val deleteWarnLevel: Map<String, String> = ${ktMap(s.deleteWarning.levels)}
    val deleteWarnKey: Map<String, String> = ${ktMap(s.deleteWarning.keys)}
}
`
}
function renderKtCaps(c) {
  const caps = c.caps.capabilities
  const v = (x) => x === null ? 'null' : typeof x === 'string' ? q(x) : String(x)
  return H('//') + `package com.aiworkdeck.contract

object ContractCapabilities {
    const val client: String = "android"
    const val backgroundUpload: Boolean = ${caps.backgroundUpload.android}
    val maxVideoSeconds: Int? = ${v(caps.maxVideoSeconds.android)}
    const val continuousSegments: Boolean = ${caps.continuousSegments.android}
    /** "true" / "false" / "runtime" */
    const val glassBlur: String = ${q(String(caps.glassBlur.android))}
    const val deviceAttestation: Boolean = ${caps.deviceAttestation.android}
    val degradedNotice: Map<String, String> = ${ktMap(Object.fromEntries(Object.entries(caps).filter(([, x]) => x.degradedNotice).map(([k, x]) => [k, x.degradedNotice])))}
}
`
}

// ---- ArkTS ----
function renderEtsTokens(c) {
  const t = c.tokens
  return H('//') + `/** 颜色为 CSS 字符串；间距单位 vp，字号单位 fp。 */
export const T = ${JSON.stringify({ L: t.L, D: t.D, S: t.S, Sp: t.Sp, Ty: t.Ty, Motion: { fastMs: t.motion.fastMs, baseMs: t.motion.baseMs, slowMs: t.motion.slowMs }, touchMin: t.touchMin, contractVersion: t.version }, null, 2)} as const
`
}
function renderEtsStrings(c) {
  return H('//') + `export const ContractStrings: Record<string, Record<string, string>> = ${JSON.stringify(c.strings, null, 2)}\n`
}
function renderEtsStates(c) {
  const s = c.state
  return H('//') + `export const ContractStates = ${JSON.stringify({
    version: s.version, states: s.states, aliases: s.aliases,
    phaseOf: Object.fromEntries(Object.entries(s.phases).flatMap(([p, v]) => v.states.map((st) => [st, p]))),
    phaseLabelKey: Object.fromEntries(Object.entries(s.phases).map(([p, v]) => [p, v.label])),
    phaseDot: Object.fromEntries(Object.entries(s.phases).map(([p, v]) => [p, v.dot])),
    failedDot: s.failedDot, stateTextKey: s.stateText, whereKey: s.whereItIs,
    retryDelaysMs: s.retryDelaysMs, maxAutoRetries: s.maxAutoRetries, events: s.events, transitions: s.transitions,
    deleteWarnOrder: s.deleteWarning.order, deleteWarnLevel: s.deleteWarning.levels, deleteWarnKey: s.deleteWarning.keys,
  }, null, 2)} as const
`
}
function renderEtsCaps(c) {
  const caps = c.caps.capabilities
  return H('//') + `export const ContractCapabilities = ${JSON.stringify({
    client: 'harmony',
    ...Object.fromEntries(Object.entries(caps).map(([k, v]) => [k, v.harmony])),
    degradedNotice: Object.fromEntries(Object.entries(caps).filter(([, x]) => x.degradedNotice).map(([k, x]) => [k, x.degradedNotice])),
  }, null, 2)} as const
`
}

/** 全部生成物：相对路径 → 内容 */
export function outputs(c) {
  const kt = 'android/contract/src/main/kotlin/com/aiworkdeck/contract'
  const ets = 'harmony/contract/src/main/ets'
  return new Map([
    ['miniprogram/styles/tokens.wxss', renderWxss(c)],
    ['miniprogram/utils/contract/states.ts', renderTsStates(c)],
    ['miniprogram/utils/contract/strings.ts', renderTsStrings(c)],
    ['miniprogram/utils/contract/capabilities.ts', renderTsCaps(c, 'miniprogram')],
    ['ios/Sources/Contract/Tokens.swift', renderSwiftTokens(c)],
    ['ios/Sources/Contract/Strings.swift', renderSwiftStrings(c)],
    ['ios/Sources/Contract/States.swift', renderSwiftStates(c)],
    ['ios/Sources/Contract/Capabilities.swift', renderSwiftCaps(c)],
    [`${kt}/Tokens.kt`, renderKtTokens(c)],
    [`${kt}/Strings.kt`, renderKtStrings(c)],
    [`${kt}/States.kt`, renderKtStates(c)],
    [`${kt}/Capabilities.kt`, renderKtCaps(c)],
    [`${ets}/Tokens.ets`, renderEtsTokens(c)],
    [`${ets}/Strings.ets`, renderEtsStrings(c)],
    [`${ets}/States.ets`, renderEtsStates(c)],
    [`${ets}/Capabilities.ets`, renderEtsCaps(c)],
  ])
}
```

- [ ] **Step 4: 写 `gen.mjs`**

```js
#!/usr/bin/env node
// contract/tools/gen.mjs — 写出四端生成物并刷新 contract.json 的 sha 清单。用法：node contract/tools/gen.mjs [--root DIR]
import { writeFileSync, mkdirSync } from 'node:fs'
import { dirname, join, resolve } from 'node:path'
import { fileURLToPath } from 'node:url'
import { loadContract, outputs, manifestFor } from './lib.mjs'

const args = process.argv.slice(2)
const i = args.indexOf('--root')
const root = resolve(i >= 0 ? args[i + 1] : join(dirname(fileURLToPath(import.meta.url)), '..', '..'))
const c = loadContract(root)
for (const [rel, content] of outputs(c)) {
  const abs = join(root, rel)
  mkdirSync(dirname(abs), { recursive: true })
  writeFileSync(abs, content)
  console.log('wrote', rel)
}
writeFileSync(join(root, 'contract', 'contract.json'), JSON.stringify(manifestFor(root, c.manifest.version), null, 2) + '\n')
console.log('refreshed contract/contract.json')
```

- [ ] **Step 5: `check.mjs` 加 ③**

在 `runChecks` 里 `checkFixtures(c, problems)` 之后加：

```js
  checkGenerated(c, problems)
```

并加函数（import 里补 `outputs`）：

```js
function checkGenerated(c, problems) {
  for (const [rel, content] of outputs(c)) {
    const abs = join(c.root, rel)
    if (!existsSync(abs)) { problems.push(`生成物缺失：${rel}（跑 node contract/tools/gen.mjs）`); continue }
    if (readFileSync(abs, 'utf8') !== content) problems.push(`生成物过期或被手改：${rel}（跑 node contract/tools/gen.mjs）`)
  }
}
```

- [ ] **Step 6: 先备份旧 Tokens.swift 的 F/A/Color 部分，再跑 gen**

把现 `ios/Sources/Design/Tokens.swift` 里 `enum F { ... }`、`enum A { ... }`、`static let touchMin`、`extension Color { init(hex:) }` 移入新文件 `ios/Sources/Design/Typography.swift`，字号改引用 `T.Ty.*`：

```swift
import SwiftUI

/// 字体与动效不是纯数据（含字重、弹簧参数），手写在这里；尺寸与时长引用生成的 T.Ty / T.Motion。
extension T {
    enum F {
        /// 只给关键数字
        static func hero() -> Font { .system(size: T.Ty.hero, weight: .light, design: .monospaced) }
        static func display() -> Font { .system(size: T.Ty.display, weight: .semibold) }
        static func title() -> Font { .system(size: T.Ty.title, weight: .semibold) }
        static func heading() -> Font { .system(size: T.Ty.heading, weight: .semibold) }
        static func body() -> Font { .system(size: T.Ty.body) }
        static func small() -> Font { .system(size: T.Ty.small) }
        static func micro() -> Font { .system(size: T.Ty.micro) }
        /// 全大写字距拉开的小标签。字距是这套排版的签名。
        static func nano() -> Font { .system(size: T.Ty.nano, weight: .medium) }
        /// 哈希、时间、坐标、计数一律等宽 —— 取证信息要能逐字符核对
        static func mono(_ size: CGFloat, _ weight: Font.Weight = .regular) -> Font {
            .system(size: size, weight: weight, design: .monospaced)
        }
    }

    /// 动效只用来表达「照片正在离开手机 / 在路上 / 已抵达」。不做装饰性动画。
    enum A {
        static let fast = Animation.easeOut(duration: T.Motion.fast)
        static let base = Animation.spring(response: 0.32, dampingFraction: 0.82)
        static let rise = Animation.spring(response: 0.42, dampingFraction: 0.86)
    }
}

extension Color {
    init(hex: UInt32) {
        self.init(.sRGB,
                  red: Double((hex >> 16) & 0xFF) / 255,
                  green: Double((hex >> 8) & 0xFF) / 255,
                  blue: Double(hex & 0xFF) / 255,
                  opacity: 1)
    }
}
```

然后删除 `ios/Sources/Design/Tokens.swift`（生成物在 `ios/Sources/Contract/Tokens.swift`，xcodegen `sources: Sources` 递归包含，无需改 project.yml）：

```bash
git rm ios/Sources/Design/Tokens.swift
node contract/tools/gen.mjs
```

- [ ] **Step 7: 跑测试与校验、iOS 编译**

Run: `node --test tests/contract-tools.test.ts && node contract/tools/check.mjs --quick && npm run typecheck`
Expected: 全过；`契约校验通过`

Run（iOS 编译确认 T.* 调用面完好）：
```bash
cd ios && xcodegen generate && xcodebuild build -scheme Workdeck -destination "platform=iOS Simulator,name=$(xcrun simctl list devices available | grep -o 'iPhone [0-9][^(]*' | head -1 | sed 's/ *$//')" -quiet && cd ..
```
Expected: BUILD SUCCEEDED

- [ ] **Step 8: 提交**

```bash
git add contract miniprogram/styles/tokens.wxss miniprogram/utils/contract ios/Sources/Contract ios/Sources/Design/Typography.swift android harmony tests/contract-tools.test.ts
git commit -m "feat(contract): gen.mjs 出四端纯数据生成物，check.mjs 校验生成物未过期；Tokens.swift 改为生成

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: 小程序接入契约与夹具

**Files:**
- Create: `miniprogram/utils/i18n.ts`、`miniprogram/utils/transition.ts`
- Modify: `miniprogram/utils/phase.ts`、`miniprogram/utils/queue.ts:16,189-191,209-223,263,295-320`、`miniprogram/pages/queue/queue.ts:161-163`、`miniprogram/pages/index/index.ts`、`miniprogram/pages/index/index.wxml:19,26,31`、`miniprogram/pages/gallery/gallery.ts`、`miniprogram/pages/gallery/gallery.wxml:38,41,43`
- Test: `tests/contract.test.ts`、`tests/phase.test.ts`（既有，必须继续过）

**Interfaces:**
- Consumes: Task 3 的 `states.ts` / `strings.ts` 导出
- Produces: `i18n.ts`：`t(key: string, vars?: Record<string, string | number>): string`；`transition.ts`：`next(state, event, attempts): QueueState | null`、`recover(state, attempts): QueueState`、`applyStatus(state, status: { delivered: boolean; waitingSeconds: number; expiresAt?: string }): { state: QueueState; waitingSeconds: number | null; expiresAt: string | null }`；`phase.ts` 新增 `deleteWarningLevel(states): { level: 'unsent'|'staged'|'landed'; n: number }`，其余导出签名不变

- [ ] **Step 1: 写夹具适配测试**

`tests/contract.test.ts`：

```ts
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { readFileSync } from 'node:fs'
import { join } from 'node:path'
import { tallyOf, tallyTotal, deleteWarningLevel, type QueueState } from '../miniprogram/utils/phase.ts'
import { next, recover, applyStatus } from '../miniprogram/utils/transition.ts'
import { t } from '../miniprogram/utils/i18n.ts'

const fx = (n: string) => JSON.parse(readFileSync(join(import.meta.dirname, '..', 'contract', 'fixtures', `${n}.json`), 'utf8'))

test('fixture: tally', () => {
  for (const k of fx('tally').cases) {
    const got = tallyOf(k.states.map((s: QueueState) => ({ state: s })))
    assert.deepEqual({ ...got, total: tallyTotal(got) }, k.expect, k.name)
  }
})

test('fixture: transitions', () => {
  for (const k of fx('transitions').cases) {
    assert.equal(next(k.from, k.event, k.attempts), k.to, `${k.from}+${k.event}(${k.attempts})`)
  }
})

test('fixture: restore', () => {
  for (const k of fx('restore').cases) assert.equal(recover(k.state, k.attempts), k.expect, `${k.state}(${k.attempts})`)
})

test('fixture: status-merge', () => {
  for (const k of fx('status-merge').cases) assert.deepEqual(applyStatus(k.state, k.status), k.expect, k.name)
})

test('fixture: delete-warning', () => {
  for (const k of fx('delete-warning').cases) assert.deepEqual(deleteWarningLevel(k.states), k.expect, JSON.stringify(k.states))
})

test('i18n: 占位符替换与缺键行为', () => {
  assert.equal(t('delete.title', { n: 3 }), '删除 3 件')
  assert.equal(t('tally.failedSuffix', { m: 2 }), '含 2 失败')
  assert.equal(t('no.such.key'), 'no.such.key')
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm test`
Expected: FAIL，`Cannot find module '../miniprogram/utils/transition.ts'`

- [ ] **Step 3: 写 `i18n.ts` 与 `transition.ts`**

`miniprogram/utils/i18n.ts`：

```ts
import { STRINGS } from './contract/strings'

/** 小程序只发大陆，固定 zh-Hans。缺键回显键名，便于走查时一眼看出没接契约。 */
export function t(key: string, vars: Record<string, string | number> = {}): string {
  const entry = STRINGS[key]
  let s = entry ? entry['zh-Hans'] : key
  for (const [k, v] of Object.entries(vars)) s = s.split(`{${k}}`).join(String(v))
  return s
}
```

`miniprogram/utils/transition.ts`：

```ts
import { MAX_AUTO_RETRIES, TRANSITIONS, type QueueState, type TransferEvent } from './contract/states'

function guardOk(guard: string | undefined, attempts: number): boolean {
  if (!guard) return true
  if (guard === 'attempts <= maxAutoRetries') return attempts <= MAX_AUTO_RETRIES
  throw new Error(`未知 guard: ${guard}`)
}

/** 迁移表驱动。无匹配规则 → null（非法迁移，调用方应拒绝并记日志）；有规则但 guard 不满足 → 原态。 */
export function next(state: QueueState, event: TransferEvent, attempts = 0): QueueState | null {
  const rules = TRANSITIONS.filter((r) => r.from === state && r.event === event)
  if (rules.length === 0) return null
  for (const r of rules) if (guardOk(r.guard, attempts)) return r.to
  return state
}

/** 冷启动回拨 = app_launch 事件；不适用的状态原样返回。 */
export function recover(state: QueueState, attempts = 0): QueueState {
  return next(state, 'app_launch', attempts) ?? state
}

export interface StatusResponse { delivered: boolean; waitingSeconds: number; expiresAt?: string }
export interface StatusMerge { state: QueueState; waitingSeconds: number | null; expiresAt: string | null }

/** status 轮询只对 uploaded 件有意义；delivered 清掉等待字段，pending 回填。 */
export function applyStatus(state: QueueState, status: StatusResponse): StatusMerge {
  if (state !== 'uploaded') return { state, waitingSeconds: null, expiresAt: null }
  if (status.delivered) return { state: next(state, 'status_delivered') ?? state, waitingSeconds: null, expiresAt: null }
  return { state: next(state, 'status_pending') ?? state, waitingSeconds: status.waitingSeconds, expiresAt: status.expiresAt ?? null }
}
```

- [ ] **Step 4: 改 `phase.ts` 走生成常量与词典**

把文件头到 `tallyTotal` 之间替换为：

```ts
/**
 * 传输阶段的展示层 —— 纯函数，不碰 wx。名称与文案全部来自 contract/（生成物在 ./contract/）。
 */
import {
  PHASE_OF, PHASE_LABEL_KEY, PHASE_DOT, FAILED_DOT, STATE_TEXT_KEY, DELETE_WARN_ORDER, DELETE_WARN_LEVEL,
  DELETE_WARN_KEY, type QueueState, type Phase,
} from './contract/states'
import { t } from './i18n'

export type { QueueState, Phase }

export const PHASE_LABEL: Record<Phase, string> = {
  uploading: t(PHASE_LABEL_KEY.uploading),
  staged: t(PHASE_LABEL_KEY.staged),
  landed: t(PHASE_LABEL_KEY.landed),
}

export function phaseOf(state: QueueState): Phase {
  return PHASE_OF[state]
}

export function stateText(state: QueueState): string {
  return t(STATE_TEXT_KEY[state])
}

/** 点色类名由契约的令牌名派生：S.waiting → dot--waiting */
export function dotClass(state: QueueState): string {
  const token = state === 'failed' ? FAILED_DOT : PHASE_DOT[phaseOf(state)]
  return `dot--${token.split('.')[1]}`
}

export interface Tally { uploading: number; failed: number; staged: number; landed: number }

export function tallyOf(items: Array<{ state: QueueState }>): Tally {
  const tl: Tally = { uploading: 0, failed: 0, staged: 0, landed: 0 }
  for (const it of items) {
    tl[phaseOf(it.state)]++
    if (it.state === 'failed') tl.failed++
  }
  return tl
}

export function tallyTotal(tl: Tally): number {
  return tl.uploading + tl.staged + tl.landed
}
```

`deleteWarning` 改为：

```ts
/** 删除确认等级：按所选里最坏的桶说话。n = 该桶件数；landed 时 n = 总数。 */
export function deleteWarningLevel(states: QueueState[]): { level: 'unsent' | 'staged' | 'landed'; n: number } {
  for (const phase of DELETE_WARN_ORDER) {
    if (phase === 'landed') break
    const n = states.filter((s) => phaseOf(s) === phase).length
    if (n > 0) return { level: DELETE_WARN_LEVEL[phase], n }
  }
  return { level: 'landed', n: states.length }
}

export function deleteWarning(states: QueueState[]): string {
  const { level, n } = deleteWarningLevel(states)
  return t(DELETE_WARN_KEY[level], { n })
}
```

其余（`projectId`、`groupByDay`、`projectsIn`）不动。

- [ ] **Step 5: 改 `queue.ts`**

- 第 16 行 `export type QueueState = ...` 改为 `export type { QueueState } from './contract/states'`（并 `import type { QueueState } from './contract/states'` 供本文件使用）。
- 第 189-191 行的 `RETRY_DELAYS_MS` / `MAX_AUTO_RETRIES` 常量删除，改为 `import { RETRY_DELAYS_MS, MAX_AUTO_RETRIES } from './contract/states'`。
- `recoverOnLaunch` 循环体改为：

```ts
  for (const it of items) {
    const to = recover(it.state, it.attempts ?? 0)
    if (to !== it.state) { it.state = to; changed = true }
  }
```

- 第 263 行 `'上传失败'` 改为 `t('state.failed')`。
- `pollStatus` 里对每件的处理改为：

```ts
      const merged = applyStatus(it.state, status)
      updateItem(it.clientMediaId, (x) => {
        x.state = merged.state
        if (merged.waitingSeconds === null) delete x.waitingSeconds; else x.waitingSeconds = merged.waitingSeconds
        if (merged.expiresAt === null) delete x.expiresAt; else x.expiresAt = merged.expiresAt
      })
      if (merged.state === 'uploaded') stillPending = true
```

顶部补 `import { recover, applyStatus } from './transition'` 与 `import { t } from './i18n'`。

- [ ] **Step 6: 页面文案走词典**

`pages/queue/queue.ts:161-163` 三个 `title` 改为 `t('queue.section.uploading')`、`t('queue.section.staged')`、`t('queue.section.landed')`，顶部 `import { t } from '../../utils/i18n'`。

`pages/index/index.ts` 与 `pages/gallery/gallery.ts` 的 `data` 加 `phaseLabel: PHASE_LABEL`（`import { PHASE_LABEL } from '../../utils/phase'`）；对应 wxml：

- `index.wxml:19` `上传中` → `{{phaseLabel.uploading}}`；`:26` `已暂存` → `{{phaseLabel.staged}}`；`:31` `已落盘` → `{{phaseLabel.landed}}`
- `gallery.wxml:38/41/43` 同样替换

- [ ] **Step 7: 跑全部测试与类型检查**

Run: `npm test && npm run typecheck && node contract/tools/check.mjs --quick`
Expected: `tests/phase.test.ts` 既有 7 例 + `tests/contract.test.ts` 6 例 + 工具测试全过；typecheck 无输出；校验通过

- [ ] **Step 8: 提交**

```bash
git add miniprogram tests/contract.test.ts
git commit -m "feat(miniprogram): 接入契约——状态/文案/常量走生成物，迁移与 status 合并改纯函数并过夹具

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: iOS 接入契约与夹具

**Files:**
- Modify: `ios/Sources/Model/CaptureItem.swift:1-60`、`ios/Sources/Model/LibraryGrouping.swift:46-56`、`ios/Sources/Design/Components.swift:45`、`ios/Sources/App/DemoData.swift:26`、`ios/Sources/Features/Queue/QueueView.swift:13,22-24,95`、`ios/Sources/Features/Library/LibraryView.swift:76,136,324-332,426`、`ios/Sources/Features/Home/HomeView.swift:139-172`、`ios/Sources/Services/UploadQueue.swift:39,59,93`、`ios/Sources/Services/Backend.swift:229`、`ios/Tests/TransferPhaseTests.swift:7,25`、`ios/project.yml:122-132`
- Create: `ios/Sources/Design/L10n.swift`、`ios/Tests/ContractFixturesTests.swift`

**Interfaces:**
- Consumes: Task 3 的 `ContractStates` / `ContractStrings`
- Produces: `func tr(_ key: String, _ vars: [String: String] = [:]) -> String`；`enum TransferEvent: String`；`TransferState.next(_ event: TransferEvent, attempts: Int = 0) -> TransferState?`；`TransferState.recovered(attempts: Int = 0) -> TransferState`；`TransferState.applyingStatus(delivered: Bool, waitingSeconds: Int64, expiresAt: String?) -> (state: TransferState, waitingSeconds: Int64?, expiresAt: String?)`；`LibraryGrouping.deleteWarningLevel(_ states: [TransferState]) -> (level: String, n: Int)`

- [ ] **Step 1: 写夹具适配测试**

`ios/Tests/ContractFixturesTests.swift`：

```swift
import XCTest
@testable import Workdeck

/// 契约夹具适配：contract/fixtures/*.json 作为测试 bundle 资源（project.yml 里挂进 WorkdeckTests）。
final class ContractFixturesTests: XCTestCase {
    private func fixture(_ name: String) throws -> [String: Any] {
        let url = Bundle(for: Self.self).resourceURL!.appendingPathComponent("fixtures/\(name).json")
        let data = try Data(contentsOf: url)
        return try JSONSerialization.jsonObject(with: data) as! [String: Any]
    }
    private func cases(_ name: String) throws -> [[String: Any]] { try fixture(name)["cases"] as! [[String: Any]] }
    private func state(_ s: String) -> TransferState { TransferState(rawValue: s)! }

    func testTally() throws {
        for k in try cases("tally") {
            let items = (k["states"] as! [String]).map { TestItems.make(state($0)) }
            let t = TransferTally.of(items)
            let e = k["expect"] as! [String: Int]
            XCTAssertEqual(t.uploading, e["uploading"], k["name"] as! String)
            XCTAssertEqual(t.failed, e["failed"], k["name"] as! String)
            XCTAssertEqual(t.staged, e["staged"], k["name"] as! String)
            XCTAssertEqual(t.landed, e["landed"], k["name"] as! String)
            XCTAssertEqual(t.total, e["total"], k["name"] as! String)
        }
    }

    func testTransitions() throws {
        for k in try cases("transitions") {
            let from = state(k["from"] as! String)
            let ev = TransferEvent(rawValue: k["event"] as! String)!
            let got = from.next(ev, attempts: k["attempts"] as! Int)
            let want = (k["to"] as? String).map(state)
            XCTAssertEqual(got, want, "\(from)+\(ev)(\(k["attempts"]!))")
        }
    }

    func testRestore() throws {
        for k in try cases("restore") {
            XCTAssertEqual(state(k["state"] as! String).recovered(attempts: k["attempts"] as! Int), state(k["expect"] as! String))
        }
    }

    func testStatusMerge() throws {
        for k in try cases("status-merge") {
            let st = k["status"] as! [String: Any]
            let got = state(k["state"] as! String).applyingStatus(
                delivered: st["delivered"] as! Bool,
                waitingSeconds: Int64(st["waitingSeconds"] as! Int),
                expiresAt: st["expiresAt"] as? String)
            let e = k["expect"] as! [String: Any]
            XCTAssertEqual(got.state, state(e["state"] as! String), k["name"] as! String)
            XCTAssertEqual(got.waitingSeconds, (e["waitingSeconds"] as? Int).map(Int64.init), k["name"] as! String)
            XCTAssertEqual(got.expiresAt, e["expiresAt"] as? String, k["name"] as! String)
        }
    }

    func testDeleteWarning() throws {
        for k in try cases("delete-warning") {
            let got = LibraryGrouping.deleteWarningLevel((k["states"] as! [String]).map(state))
            let e = k["expect"] as! [String: Any]
            XCTAssertEqual(got.level, e["level"] as! String)
            XCTAssertEqual(got.n, e["n"] as! Int)
        }
    }

    func testLegacyMovingDecodes() throws {
        let decoded = try JSONDecoder().decode([TransferState].self, from: Data(#"["moving","waiting"]"#.utf8))
        XCTAssertEqual(decoded, [.uploading, .waiting])
        let encoded = String(data: try JSONEncoder().encode([TransferState.uploading]), encoding: .utf8)
        XCTAssertEqual(encoded, #"["uploading"]"#)
    }

    func testStringsComeFromContract() {
        XCTAssertEqual(TransferPhase.landed.caption, tr("phase.landed"))
        XCTAssertEqual(TransferState.failed.caption, "上传失败")
        XCTAssertEqual(tr("delete.title", ["n": "3"]), "删除 3 件")
    }
}
```

- [ ] **Step 2: project.yml 把夹具挂进测试 bundle**

`ios/project.yml` 的 `WorkdeckTests.sources` 改为：

```yaml
    sources:
      - path: Tests
      - path: ../contract/fixtures
        type: folder
        buildPhase: resources
```

- [ ] **Step 3: 跑测试确认失败**

Run:
```bash
cd ios && xcodegen generate && xcodebuild test -scheme Workdeck -only-testing:WorkdeckTests/ContractFixturesTests -destination "platform=iOS Simulator,name=$(xcrun simctl list devices available | grep -o 'iPhone [0-9][^(]*' | head -1 | sed 's/ *$//')" -quiet; cd ..
```
Expected: 编译失败，`TransferEvent`、`tr` 未定义

- [ ] **Step 4: 写 `L10n.swift`**

```swift
import Foundation

/// 契约文案。国际版设备语言非中文且 en 非空时用 en，否则 zh-Hans。缺键回显键名。
func tr(_ key: String, _ vars: [String: String] = [:]) -> String {
    let entry = ContractStrings.table[key]
    let preferZh = (Locale.preferredLanguages.first ?? "zh").hasPrefix("zh")
    var s = entry?["zh-Hans"] ?? key
    if !preferZh, let en = entry?["en"], !en.isEmpty { s = en }
    for (k, v) in vars { s = s.replacingOccurrences(of: "{\(k)}", with: v) }
    return s
}
```

- [ ] **Step 5: 改 `CaptureItem.swift` 前 60 行**

替换 `TransferState` 与 `TransferPhase` 为：

```swift
/// 一张影像在「拍摄 → 抵达电脑」这条链上的位置。名称、别名、映射、文案键全部来自 contract/（ContractStates）。
enum TransferState: String, Codable, Sendable, CaseIterable {
    case waiting, uploading, uploaded, arrived, failed

    /// 旧版本落盘的 `moving` 按契约别名解码为 uploading；编码始终用正式名。
    init(from decoder: Decoder) throws {
        let raw = try decoder.singleValueContainer().decode(String.self)
        let canonical = ContractStates.aliases[raw] ?? raw
        guard let s = TransferState(rawValue: canonical) else {
            throw DecodingError.dataCorrupted(.init(codingPath: decoder.codingPath, debugDescription: "未知状态 \(raw)"))
        }
        self = s
    }

    var caption: String { tr(ContractStates.stateTextKey[rawValue]!) }
    var whereItIs: String { tr(ContractStates.whereKey[rawValue]!) }
    var phase: TransferPhase { TransferPhase(rawValue: ContractStates.phaseOf[rawValue]!)! }

    /// 迁移表驱动。无规则 → nil（非法迁移）；有规则但 guard 不满足 → 原态。
    func next(_ event: TransferEvent, attempts: Int = 0) -> TransferState? {
        let rules = ContractStates.transitions.filter { $0.from == rawValue && $0.event == event.rawValue }
        if rules.isEmpty { return nil }
        for r in rules where TransferState.guardOK(r.guard, attempts: attempts) { return TransferState(rawValue: r.to)! }
        return self
    }

    /// 冷启动回拨 = app_launch 事件。iOS 暂无 attempts 计数，调用方传 0。
    func recovered(attempts: Int = 0) -> TransferState { next(.appLaunch, attempts: attempts) ?? self }

    /// status 轮询只对 uploaded 有意义；delivered 清掉等待字段，pending 回填。
    func applyingStatus(delivered: Bool, waitingSeconds: Int64, expiresAt: String?)
        -> (state: TransferState, waitingSeconds: Int64?, expiresAt: String?) {
        guard self == .uploaded else { return (self, nil, nil) }
        if delivered { return (next(.statusDelivered) ?? self, nil, nil) }
        return (next(.statusPending) ?? self, waitingSeconds, expiresAt)
    }

    private static func guardOK(_ g: String?, attempts: Int) -> Bool {
        guard let g else { return true }
        if g == "attempts <= maxAutoRetries" { return attempts <= ContractStates.maxAutoRetries }
        preconditionFailure("未知 guard: \(g)")
    }
}

enum TransferEvent: String, Sendable {
    case kick, http2xx = "http_2xx", httpError = "http_error", networkError = "network_error"
    case retryManual = "retry_manual", retryAuto = "retry_auto"
    case statusDelivered = "status_delivered", statusPending = "status_pending", appLaunch = "app_launch"
}

enum TransferPhase: String, CaseIterable, Sendable {
    case uploading, staged, landed
    var caption: String { tr(ContractStates.phaseLabelKey[rawValue]!) }
}
```

- [ ] **Step 6: 全仓 `.moving` → `.uploading`**

逐处改：`Components.swift:45`、`DemoData.swift:26`、`QueueView.swift:13,95`、`LibraryView.swift:136`、`UploadQueue.swift:39,59,93`、`Tests/TransferPhaseTests.swift:7,25`。`UploadQueue.swift:41` 的回拨改为 `to: s.state.recovered()`（语义不变，走契约）。确认：

```bash
grep -rn "\.moving\b" ios/Sources ios/Tests | grep -v "T\.S\.moving\|movingOnDark"
```
Expected: 无输出

- [ ] **Step 7: 内联文案改键**

- `HomeView.swift:139/141/143`：`"上传中"` → `TransferPhase.uploading.caption`，`"已暂存"` → `TransferPhase.staged.caption`，`"已落盘"` → `TransferPhase.landed.caption`
- `HomeView.swift:158`：改为
  ```swift
  let f = t.failed > 0 ? tr("tally.summary.failedPart", ["m": String(t.failed)]) : ""
  return tr("tally.summary", ["u": String(t.uploading), "f": f, "s": String(t.staged), "l": String(t.landed)])
  ```
- `HomeView.swift:172`、`LibraryView.swift:326`：`Text("含 \(x) 失败")` → `Text(tr("tally.failedSuffix", ["m": String(x)]))`
- `LibraryView.swift:76,426`：`"删除 \(selected.count) 件"` → `tr("delete.title", ["n": String(selected.count)])`
- `LibraryView.swift:324/331/332`：同 HomeView 用 `TransferPhase.*.caption`
- `QueueView.swift:22-24`：三个标题改 `tr("queue.section.uploading")`、`tr("queue.section.staged")`、`tr("queue.section.landed")`
- `Backend.swift:229`：`"上传失败（\(sc)）"` → `tr("error.uploadFailed", ["code": String(sc)])`
- `LibraryGrouping.swift:46-56` 替换为：

```swift
    /// 删除确认等级：按所选里最坏的桶说话。n = 该桶件数；landed 时 n = 总数。
    static func deleteWarningLevel(_ states: [TransferState]) -> (level: String, n: Int) {
        for phase in ContractStates.deleteWarnOrder where phase != "landed" {
            let n = states.filter { $0.phase.rawValue == phase }.count
            if n > 0 { return (ContractStates.deleteWarnLevel[phase]!, n) }
        }
        return ("landed", states.count)
    }

    static func deleteWarning(for items: [CaptureItem]) -> String {
        let (level, n) = deleteWarningLevel(items.map(\.state))
        return tr(ContractStates.deleteWarnKey[level]!, ["n": String(n)])
    }
```

- [ ] **Step 8: 跑 iOS 全部单测**

Run:
```bash
cd ios && xcodegen generate && xcodebuild test -scheme Workdeck -only-testing:WorkdeckTests -destination "platform=iOS Simulator,name=$(xcrun simctl list devices available | grep -o 'iPhone [0-9][^(]*' | head -1 | sed 's/ *$//')" -quiet; cd ..
```
Expected: TEST SUCCEEDED，含既有 `TransferPhaseTests`、`LibraryGroupingTests` 与新增 7 例

- [ ] **Step 9: 提交**

```bash
git add ios
git commit -m "feat(ios): 接入契约——moving 改名 uploading 加解码别名，文案走词典，迁移/回拨/status 合并走迁移表并过夹具

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 6: 内联文案校验（⑤）

**Files:**
- Modify: `contract/tools/check.mjs`
- Test: `tests/contract-tools.test.ts`

**Interfaces:**
- Produces: `check.mjs` ⑤ 扫描 `ios/Sources/**/*.swift`、`miniprogram/pages/**/*.{ts,wxml}`、`miniprogram/utils/**/*.ts`（排除 `miniprogram/utils/contract/`），对词典里**不含 `{` 的** zh-Hans 值匹配 `"值"`、`'值'`、`>值<` 三种形态，命中即红；`--quick` 跳过

- [ ] **Step 1: 写失败测试**

追加到 `tests/contract-tools.test.ts`：

```ts
test('check：现状全量（含内联扫描）全绿', () => {
  const r = runChecks(ROOT, { quick: false })
  assert.deepEqual(r.problems, [])
})

test('check：内联词典文案要红', () => {
  const dir = tempCopy()
  for (const rel of outputs(loadContract(ROOT)).keys()) {
    const src = join(ROOT, rel)
    if (existsSync(src)) cpSync(src, join(dir, rel))
  }
  cpSync(join(ROOT, 'contract', 'tools', 'wxss-footer.css'), join(dir, 'contract', 'tools', 'wxss-footer.css'))
  const p = join(dir, 'ios', 'Sources', 'Features', 'Foo.swift')
  cpSync(join(ROOT, 'ios', 'Sources', 'Design', 'L10n.swift'), p)  // 任意 swift 文件做载体
  writeFileSync(p, readFileSync(p, 'utf8') + '\nlet x = "已落盘"\n')
  const r = runChecks(dir, { quick: false })
  assert.ok(r.problems.some((m) => m.includes('Foo.swift') && m.includes('内联')), r.problems.join('\n'))
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/contract-tools.test.ts`
Expected: 第二例 FAIL（现在 ⑤ 还不存在，problems 为空）

- [ ] **Step 3: 实现 ⑤**

`check.mjs` 加：

```js
function walk(dir, exts, skip, out = []) {
  if (!existsSync(dir)) return out
  for (const e of readdirSync(dir, { withFileTypes: true })) {
    const p = join(dir, e.name)
    if (skip.some((s) => p.includes(s))) continue
    if (e.isDirectory()) walk(p, exts, skip, out)
    else if (exts.some((x) => e.name.endsWith(x))) out.push(p)
  }
  return out
}

function checkInline(c, problems) {
  const values = Object.values(c.strings).map((v) => v['zh-Hans']).filter((v) => !v.includes('{'))
  const files = [
    ...walk(join(c.root, 'ios', 'Sources'), ['.swift'], ['/Contract/']),
    ...walk(join(c.root, 'miniprogram', 'pages'), ['.ts', '.wxml'], []),
    ...walk(join(c.root, 'miniprogram', 'utils'), ['.ts'], ['/utils/contract/']),
  ]
  for (const f of files) {
    const text = readFileSync(f, 'utf8')
    for (const v of values) {
      for (const form of [`"${v}"`, `'${v}'`, `>${v}<`]) {
        if (text.includes(form)) problems.push(`内联文案 ${form} 于 ${f.slice(c.root.length + 1)}：改用 strings.json 的键`)
      }
    }
  }
}
```

`runChecks` 里加 `if (!quick) checkInline(c, problems)`。

- [ ] **Step 4: 跑全量校验，修掉暴露出来的残留内联**

Run: `node contract/tools/check.mjs`
如有输出，逐条改成 `tr(...)` / `t(...)`，直到：
Expected: `契约校验通过`

Run: `node --test tests/contract-tools.test.ts && npm test`
Expected: 全过

- [ ] **Step 5: 提交**

```bash
git add contract/tools/check.mjs tests/contract-tools.test.ts ios miniprogram
git commit -m "feat(contract): check.mjs 扫描四端源码内联词典文案

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 7: 五层触发（CLAUDE.md、skill、钩子、pre-commit、CI）

**Files:**
- Create: `CLAUDE.md`、`.claude/skills/contract-change/SKILL.md`、`.claude/settings.json`、`contract/tools/hook-post-edit.mjs`、`scripts/install-hooks.mjs`、`.github/workflows/contract.yml`、`.github/pull_request_template.md`、`contract/README.md`
- Modify: `package.json`（`prepare`）、`.gitignore`、`README.md`（一致性口径段落）

**Interfaces:**
- Produces: `hook-post-edit.mjs` 读 stdin JSON（Claude Code PostToolUse 载荷），`tool_input.file_path` 命中 `contract/`、`miniprogram/utils/contract/`、`ios/Sources/Contract/`、`miniprogram/styles/tokens.wxss`、`android/contract/`、`harmony/contract/` 时跑 `check.mjs --quick`；失败 → exit 2 且 stderr 输出问题（Claude Code 会把 stderr 回灌给模型），成功 → exit 0 静默

- [ ] **Step 1: `.gitignore` 确认只忽略 worktrees**

```bash
grep -n "\.claude" .gitignore || true
```
若有整行 `.claude/` 则改为 `.claude/worktrees/`；若没有则追加 `.claude/worktrees/`。

- [ ] **Step 2: 写 `CLAUDE.md`（仓库根）**

```markdown
# aiworkdeck-mobile

四个原生端（iOS Swift / 微信小程序 / 安卓 Kotlin / 鸿蒙 ArkTS）共享一份契约。
设计见 `docs/specs/2026-09-02-contract-design.md`，操作步骤见 `contract/README.md`。

## 契约规矩（每次改动前先读）

1. 状态名、桶名、界面文案、设计令牌、能力开关、API 形状的**唯一来源**是 `contract/`。
   不要在 Swift / TS / Kotlin / ArkTS 里直接改这些值。
2. 改动顺序：先改 `contract/*.json` 与 `contract/fixtures/`，跑 `node contract/tools/gen.mjs`，
   再改各端行为代码；生成物（文件头带 GENERATED）进仓提交，不要手改生成物。
3. 取证主流程（相机、队列、图集、归档确认）的界面文案不许内联，一律走 `strings.json` 的键：
   iOS `tr("key")`、小程序 `t('key')`。`check.mjs` 会扫出内联。
4. 提交前 `node contract/tools/check.mjs` 必须绿（pre-commit 与 CI 都会再跑）。
5. API 变更先改服务端仓 `backend/src/main/resources/openapi/mobile-v1.yaml` 并过
   `MobileApiContractTest`，再在本仓 `node contract/tools/pull-api.mjs` 刷新副本。
6. 新端（安卓、鸿蒙）从生成物起步；先写一个夹具适配测试，再写第一屏。

涉及文案 / 令牌 / 状态 / 能力 / 接口的任务，先用 `contract-change` skill。

## 本地验证

- 小程序：`npm ci && npm run typecheck && npm test`
- iOS：`cd ios && xcodegen generate && xcodebuild test -scheme Workdeck -only-testing:WorkdeckTests -destination "platform=iOS Simulator,name=<可用 iPhone>"`
- 契约：`node contract/tools/check.mjs`
```

- [ ] **Step 3: 写项目级 skill**

`.claude/skills/contract-change/SKILL.md`：

```markdown
---
name: contract-change
description: 改动界面文案、设计令牌、传输状态、能力开关或 /api/mobile 接口形状时必须使用。契约是四端唯一来源，本 skill 给出先改 contract/ 再生成再改各端的固定步骤。
---

# 改契约

触发词：文案、措辞、改成、令牌、颜色、字号、间距、状态、桶、上传中/已暂存/已落盘、能力、后台上传、接口字段。

## 步骤

1. 定位要改的东西属于哪个文件：
   - 文案 → `contract/strings.json`（zh-Hans 与 en 两栏都改）
   - 颜色 / 间距 / 字号 → `contract/tokens.json`
   - 状态名 / 桶 / 迁移 / 重试 → `contract/transfer-state.json`，同时改 `contract/fixtures/` 里相关期望
   - 能力 → `contract/capabilities.json`
   - 接口 → 服务端仓 `openapi/mobile-v1.yaml`（先过服务端 `MobileApiContractTest`），再 `node contract/tools/pull-api.mjs`
2. `node contract/tools/gen.mjs`
3. 只在行为层改各端代码（迁移逻辑、界面绑定），不改生成物、不内联文案。
4. `node contract/tools/check.mjs && npm test`；改了 iOS 再跑 WorkdeckTests。
5. 提交信息写明改了契约哪一项。

## 不要

- 直接改 `ios/Sources/Contract/*`、`miniprogram/utils/contract/*`、`miniprogram/styles/tokens.wxss`
- 在 Swift / TS 里写中文字面量表达状态或桶
- 改状态落盘字符串而不加别名
```

- [ ] **Step 4: 写钩子与 settings**

`contract/tools/hook-post-edit.mjs`：

```js
#!/usr/bin/env node
// Claude Code PostToolUse 钩子：编辑命中契约或生成物路径时立刻校验。失败 exit 2 并把问题写到 stderr（会回灌给模型）。
import { readFileSync } from 'node:fs'
import { runChecks } from './check.mjs'

const WATCH = ['contract/', 'miniprogram/utils/contract/', 'ios/Sources/Contract/', 'miniprogram/styles/tokens.wxss', 'android/contract/', 'harmony/contract/']
let payload = {}
try { payload = JSON.parse(readFileSync(0, 'utf8')) } catch { process.exit(0) }
const fp = String(payload?.tool_input?.file_path ?? '')
if (!WATCH.some((w) => fp.includes(w))) process.exit(0)
const r = runChecks(process.cwd(), { quick: true })
if (!r.ok) {
  console.error('契约校验失败（contract-change skill 步骤 2：先跑 node contract/tools/gen.mjs）：\n' + r.problems.map((p) => ' - ' + p).join('\n'))
  process.exit(2)
}
```

`.claude/settings.json`：

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [{ "type": "command", "command": "node contract/tools/hook-post-edit.mjs" }]
      }
    ]
  }
}
```

- [ ] **Step 5: pre-commit 安装脚本**

`scripts/install-hooks.mjs`：

```js
#!/usr/bin/env node
// npm prepare 时安装 pre-commit：跑契约校验。worktree 下 hooks 在 common dir。
import { execSync } from 'node:child_process'
import { writeFileSync, mkdirSync, chmodSync } from 'node:fs'
import { join } from 'node:path'

let common
try { common = execSync('git rev-parse --git-common-dir', { encoding: 'utf8' }).trim() } catch { process.exit(0) }
const hooks = join(common, 'hooks')
mkdirSync(hooks, { recursive: true })
const p = join(hooks, 'pre-commit')
writeFileSync(p, `#!/bin/sh
# 由 scripts/install-hooks.mjs 安装。契约校验不过不许提交。
node contract/tools/check.mjs || { echo "契约校验失败，见上。改 contract/ 后跑 node contract/tools/gen.mjs"; exit 1; }
`)
chmodSync(p, 0o755)
console.log('installed', p)
```

`package.json` scripts 加 `"prepare": "node scripts/install-hooks.mjs"`。Run: `npm run prepare`。

- [ ] **Step 6: CI 与 PR 模板**

`.github/workflows/contract.yml`：

```yaml
name: contract
on:
  push:
    branches: [main]
  pull_request:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: node contract/tools/check.mjs
      - run: npm run typecheck
      - run: npm test
```

`.github/pull_request_template.md`：

```markdown
## 改了什么

## 契约
- [ ] 没碰文案 / 令牌 / 状态 / 能力 / 接口
- [ ] 碰了：已先改 `contract/` 与夹具 → `gen.mjs` → 各端；`check.mjs` 绿
```

- [ ] **Step 7: `contract/README.md` 与主 README**

`contract/README.md`：

```markdown
# contract/ — 四端共享契约

真源。四端不共享 UI 代码，共享这里的数据与夹具。设计：`docs/specs/2026-09-02-contract-design.md`。

| 文件 | 管什么 |
|---|---|
| `transfer-state.json` | 五态、别名、三桶、迁移表、重试、删除警告等级 |
| `strings.json` | 文案 key → {zh-Hans, en} |
| `tokens.json` | 设计令牌（基准 pt；wxss ×2 rpx；字号 px） |
| `capabilities.json` | 四端能力声明与降级提示键 |
| `api/mobile-v1.yaml` + `PINNED.json` | 服务端 OpenAPI 的钉版副本 |
| `fixtures/` | 黄金向量，四端各自适配跑 |

## 改法

1. 改 JSON（改状态机时同步改 `fixtures/`）
2. `node contract/tools/gen.mjs`
3. 改各端行为代码
4. `node contract/tools/check.mjs && npm test`（iOS 另跑 WorkdeckTests）

## 生成物

`miniprogram/styles/tokens.wxss`、`miniprogram/utils/contract/`、`ios/Sources/Contract/`、
`android/contract/`、`harmony/contract/`。文件头带 GENERATED，不要手改。
```

主 `README.md`「一致性口径」段的第一句后追加一行：

```markdown
契约的机器形态在 `contract/`（真源）；改动规矩见根目录 `CLAUDE.md`。
```

- [ ] **Step 8: 验证五层**

```bash
node contract/tools/check.mjs
echo '{"tool_input":{"file_path":"contract/strings.json"}}' | node contract/tools/hook-post-edit.mjs; echo "hook exit=$?"
ls -la "$(git rev-parse --git-common-dir)/hooks/pre-commit"
```
Expected: `契约校验通过`；`hook exit=0`；pre-commit 文件存在可执行

再做一次反向验证：临时把 `contract/strings.json` 某个 zh-Hans 改一字不跑 gen，重复上面钩子命令，Expected `hook exit=2` 且 stderr 列出 sha 过期；然后 `git checkout contract/strings.json`。

- [ ] **Step 9: 提交**

```bash
git add CLAUDE.md .claude/settings.json .claude/skills scripts/install-hooks.mjs contract/tools/hook-post-edit.mjs contract/README.md .github/workflows/contract.yml .github/pull_request_template.md package.json .gitignore README.md
git commit -m "chore(contract): 五层触发——CLAUDE.md 规矩、contract-change skill、编辑钩子、pre-commit、CI

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 8: API 副本钉版（依赖服务端计划完成）

**Files:**
- Create: `contract/tools/pull-api.mjs`、`contract/api/mobile-v1.yaml`（拉取产物）、`contract/api/PINNED.json`
- Modify: `contract/tools/check.mjs`（加 ④）
- Test: `tests/contract-tools.test.ts`

**Interfaces:**
- Consumes: 服务端仓 `backend/src/main/resources/openapi/mobile-v1.yaml`（服务端计划 Task 1 产出）
- Produces: `PINNED.json = { serverCommit: string, sha256: string, pulledAt: string }`；`check.mjs` ④：副本存在时 sha 必须等于 PINNED；副本不存在时输出警告不算失败（服务端计划完成前的过渡）
- 环境变量 `CHECKBA_CLOUD_DIR` 覆盖服务端仓路径，默认 `/Users/zewei/Documents/2024-2044/5-Tech/1-2 checkba_cloud`

- [ ] **Step 1: 写失败测试**

```ts
test('check：API 副本 sha 与 PINNED 不一致要红', () => {
  const dir = tempCopy()
  for (const rel of outputs(loadContract(ROOT)).keys()) {
    const src = join(ROOT, rel)
    if (existsSync(src)) cpSync(src, join(dir, rel))
  }
  cpSync(join(ROOT, 'contract', 'tools', 'wxss-footer.css'), join(dir, 'contract', 'tools', 'wxss-footer.css'))
  const y = join(dir, 'contract', 'api', 'mobile-v1.yaml')
  writeFileSync(y, readFileSync(y, 'utf8') + '\n# tampered\n')
  const r = runChecks(dir, { quick: true })
  assert.ok(r.problems.some((m) => m.includes('mobile-v1.yaml') && m.includes('PINNED')), r.problems.join('\n'))
})
```

- [ ] **Step 2: 写 `pull-api.mjs`**

```js
#!/usr/bin/env node
// 从本机服务端仓拉 OpenAPI 副本并写 PINNED.json。用法：CHECKBA_CLOUD_DIR=... node contract/tools/pull-api.mjs
import { readFileSync, writeFileSync, mkdirSync } from 'node:fs'
import { join, dirname, resolve } from 'node:path'
import { fileURLToPath } from 'node:url'
import { execSync } from 'node:child_process'
import { sha256 } from './lib.mjs'

const root = resolve(join(dirname(fileURLToPath(import.meta.url)), '..', '..'))
const server = process.env.CHECKBA_CLOUD_DIR ?? '/Users/zewei/Documents/2024-2044/5-Tech/1-2 checkba_cloud'
const src = join(server, 'backend', 'src', 'main', 'resources', 'openapi', 'mobile-v1.yaml')
const yaml = readFileSync(src, 'utf8')
const commit = execSync('git rev-parse HEAD', { cwd: server, encoding: 'utf8' }).trim()
mkdirSync(join(root, 'contract', 'api'), { recursive: true })
writeFileSync(join(root, 'contract', 'api', 'mobile-v1.yaml'), yaml)
writeFileSync(join(root, 'contract', 'api', 'PINNED.json'),
  JSON.stringify({ serverCommit: commit, sha256: sha256(yaml), pulledAt: new Date().toISOString() }, null, 2) + '\n')
console.log('pinned', commit.slice(0, 12))
```

- [ ] **Step 3: `check.mjs` 加 ④**

```js
function checkApiPin(c, problems) {
  const y = join(c.root, 'contract', 'api', 'mobile-v1.yaml')
  const p = join(c.root, 'contract', 'api', 'PINNED.json')
  if (!existsSync(y) || !existsSync(p)) { console.warn('⚠ contract/api 尚无副本，跳过 ④（服务端计划完成后跑 pull-api.mjs）'); return }
  const pinned = JSON.parse(readFileSync(p, 'utf8'))
  if (sha256(readFileSync(y, 'utf8')) !== pinned.sha256) problems.push('contract/api/mobile-v1.yaml 的 sha 与 PINNED.json 不一致：副本被手改，重跑 pull-api.mjs')
}
```

`runChecks` 里 `checkGenerated` 之后加 `checkApiPin(c, problems)`；import 补 `sha256`。

- [ ] **Step 4: 拉副本并跑全部**

Run: `node contract/tools/pull-api.mjs && node contract/tools/check.mjs && node --test tests/contract-tools.test.ts`
Expected: `pinned <sha>`；`契约校验通过`；测试全过

- [ ] **Step 5: 提交**

```bash
git add contract
git commit -m "feat(contract): 钉版服务端 OpenAPI 副本（pull-api.mjs + PINNED），check.mjs 校验未漂

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 9: 收尾——PR、看板、记忆

- [ ] **Step 1: 全量验证**

```bash
npm ci && node contract/tools/check.mjs && npm run typecheck && npm test
cd ios && xcodegen generate && xcodebuild test -scheme Workdeck -only-testing:WorkdeckTests -destination "platform=iOS Simulator,name=$(xcrun simctl list devices available | grep -o 'iPhone [0-9][^(]*' | head -1 | sed 's/ *$//')" -quiet; cd ..
```
Expected: 全绿

- [ ] **Step 2: C6 验收（设计 §13 最后一条）**

在仓库根新开一个 Claude Code 会话，只说「把已暂存改成已缓存」。合格标准：会话先改 `contract/strings.json` 两栏，跑 `gen.mjs`，不去改 Swift/TS 字面量；`check.mjs` 绿。不合格则回到 Task 7 调整 CLAUDE.md 与 skill 措辞。

- [ ] **Step 3: 开 PR**

```bash
git push -u origin HEAD
gh pr create --title "feat(contract): 四端共享契约——真源、生成、夹具与五层触发（dev-board#392）" --body "$(cat <<'EOF'
## 摘要
- `contract/` 成为状态机 / 文案 / 令牌 / 能力 / API 形状的唯一来源
- `gen.mjs` 出四端纯数据生成物；`check.mjs` 五项校验；五个黄金夹具两端各自适配
- iOS `moving → uploading` 加解码别名；两端文案抽进词典；Tokens.swift / tokens.wxss 改为生成
- CLAUDE.md 规矩、contract-change skill、编辑钩子、pre-commit、CI 五层触发

设计 `docs/specs/2026-09-02-contract-design.md`，计划 `docs/superpowers/plans/2026-09-02-contract-mobile.md`。

## 复测提示
新开会话说「把已暂存改成已缓存」，应先改 strings.json 再 gen。

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 4: 看板落实记录**

用 `/tmp/board.sh`（获取方式见全局 CLAUDE.md §6）在契约卡上评论落实记录（落实情况 / PR 链接 / 模型 / 起止时间 / 分支 / 自验证 / 复测提示），状态改「待复测」。
