/* GENERATED by contract/tools/gen.mjs from contract/*.json — 不要手改，改 contract/ 再跑 gen */
page {
  --bg: #FCFBF7;
  --bg-sunken: #F1EFE7;
  --fg: #2B2A26;
  --fg-muted: #6B675C;
  --fg-faint: #A8A296;
  --rule: #EAE7DD;
  --rule-strong: #DDD8CA;
  --accent: #2E5A50;
  --accent-wash: #E3EDE9;
  --dk-bg: #191713;
  --dk-surface: #221F1A;
  --dk-fg: #EDE9DF;
  --dk-fg-muted: #B0AA9C;
  --dk-rule: rgba(255, 255, 255, 0.10);
  --st-waiting: #8A6320;
  --st-moving: #3C5A73;
  --st-arrived: #2E5A50;
  --st-failed: #B5483C;
  --st-waiting-dk: #D19C3D;
  --st-moving-dk: #93B3C6;
  --st-arrived-dk: #89A8A0;
  --s1: 8rpx;
  --s2: 16rpx;
  --s3: 24rpx;
  --s4: 32rpx;
  --s5: 40rpx;
  --s6: 48rpx;
  --s8: 64rpx;
  --s10: 80rpx;
  --s16: 128rpx;
  --gutter: 40rpx;
  --t-hero: 44px;
  --t-display: 30px;
  --t-title: 21px;
  --t-heading: 16px;
  --t-body: 15px;
  --t-small: 13px;
  --t-micro: 11px;
  --t-nano: 10px;
  --font-sans: system-ui, -apple-system, "PingFang SC", "HarmonyOS Sans SC", "Source Han Sans SC", "Helvetica Neue", sans-serif;
  --font-mono: "SF Mono", ui-monospace, Menlo, Consolas, monospace;
  --d-fast: 140ms;
  --d-base: 220ms;
  --d-slow: 320ms;
  --e-out: cubic-bezier(0.22, 1, 0.36, 1);
  --e-in: cubic-bezier(0.55, 0, 1, 0.45);
  --touch-min: 88rpx;
}

/* 全大写小标签。字距拉开是这套排版的签名，别改。 */
.label {
  font-size: var(--t-nano);
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--fg-faint);
  font-weight: 500;
}

.mono {
  font-family: var(--font-mono);
  font-variant-numeric: tabular-nums;
}

.rule {
  height: 1rpx;
  background: var(--rule);
}
